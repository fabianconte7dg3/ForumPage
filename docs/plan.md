# Plan y estado — Forum Foundation

> Resumen de estado vivo. La fuente completa de checklists es [02-plan-de-ejecucion.md](../02-plan-de-ejecucion.md). **Actualizar este archivo cada vez que se cierre un bloque o cambie de fase** — es lo que evita re-explicar "dónde vamos" en cada sesión nueva.

## Estado actual

**Fase:** 1 — Base pública (cerrada) + 2 — Centro de Aprendizaje (cerrada) + 3 — Portal del Becario / Staff (cerrada & optimizada).
**Última actualización:** 2026-08-04 (Datos reales de comunidades/actividades/equipo cargados en producción — ver Fase 1, Paso T)

Fase 0 tiene sus entregables iniciales listos (ver abajo). **Ya existe un servidor real** (VPS de AWS, `https://volumetrix.servegame.com`, ver Fase 1 Paso Q) sirviendo la app con HTTPS real, Postgres migrado, y **contenido real ya cargado**: 36 comunidades, 2 sedes, 4 centros educativos, 71 actividades/historias, 8 del equipo, y sus 3316 archivos de imágenes reales (ver Fase 1 Paso T). No es el droplet definitivo de DigitalOcean que describe `01-documento-de-proyecto.md` §12 — es un VPS que ya tenía el usuario, usado mientras se define el presupuesto final; el proceso de despliegue (`scripts/deploy.sh`) no depende de cuál sea el servidor. **Sigue pendiente**: becarios reales (sin consentimiento firmado todavía, producción está en 0 a propósito), programas/proyectos reales (los de dev eran ficticios, no se llevaron), informes anuales/podcast, y una cuenta de staff real (nadie creó todavía el primer usuario de producción).

## Principios de ejecución (no negociables)

1. **El contenido va antes que las funciones lucidoras.** El Mapa de Impacto no se construye hasta que haya comunidades, actividades y becarios cargados — de lo contrario es una pantalla vacía.
2. **Lo que depende de terceros arranca el primer día.** Consentimientos de imagen, inventario de artículos, coordenadas de comunidades: dependen de personas, no de código. Si esperan a su fase, la bloquean.
3. **Cada fase termina en algo desplegado y usable.**
4. **El sitio WordPress actual no se toca hasta el final.** El cambio de DNS es un evento único, al cierre de Fase 1.

## Definición de "hecho" (aplica a toda fase/módulo)

- [ ] Funciona en español e inglés, selector conduce a la página equivalente
- [ ] Se ve y opera bien en un teléfono de gama baja
- [ ] El staff crea/edita/borra sus registros desde el panel sin ayuda
- [ ] No supera 500 KB en primera carga
- [ ] Control de acceso según la matriz de [spec.md](spec.md#control-de-acceso-iam)
- [ ] Documentado en el `README` de operaciones

## Fases

### Fase 0 — Preparación *(sin código, bloquea todo lo demás)*

Entregables ya producidos, pendientes de llenar con datos reales del staff:

- [docs/fase-0/consentimiento.md](fase-0/consentimiento.md) — borrador bilingüe del formulario de consentimiento (becario + acudiente de menor), pendiente de validación legal
- [docs/fase-0/plantillas/](fase-0/plantillas/) — CSV para comunidades, sedes, centros educativos, programas y becarios
- [docs/fase-0/entrevista-staff.md](fase-0/entrevista-staff.md) — guion de entrevista a quien publicará en el sitio
- [docs/fase-0/accesos.md](fase-0/accesos.md) — checklist vivo de cuentas y accesos

Pendiente (depende de personas, no de código):

- [x] Inventario completo de artículos del WordPress actual — ver [docs/fase-0/accesos.md](fase-0/accesos.md)
- [ ] Informes anuales y podcast descargados de Google Drive / Anchor
- [x] Comunidades, sedes, centros educativos, actividades/historias y equipo — datos reales ya en producción (2026-08-04, ver Fase 1 Paso T). **Becarios sigue pendiente**: sin consentimiento firmado todavía, no se puede publicar ninguno real — producción hoy está intencionalmente en 0.
- [ ] Validar el consentimiento con asesoría legal panameña y **empezar la recolección de firmas** — punto crítico del cronograma
- [x] Realizar la entrevista al staff y completar el resumen en [entrevista-staff.md](fase-0/entrevista-staff.md) — hallazgo clave: el sitio anterior murió porque solo el creador de WordPress tenía control, confirmando el riesgo de "mantenedor único"; volumen real ~20 actividades/mes; staff pidió campos avanzados visibles (ajustado en `04-diseno-y-sistema-visual.md §8.1`)
- [ ] Ir tachando `docs/fase-0/accesos.md` conforme existan las cuentas reales

**Criterio de cierre:** hoja de cálculo con comunidades/coordenadas + inventario de artículos + al menos un consentimiento firmado modelo.

### Fase 1 — Base pública

Infraestructura (Docker Compose, Payload, Postgres, Caddy) → colecciones base → Actividades y migración desde WordPress → sitio público (home, mural, fichas) → Mapa de Impacto → tutorías y panel de impacto → lanzamiento con redirecciones 301 y cambio de DNS.

Criterio de aceptación clave: staff publica una actividad con 3 fotos desde el teléfono en **menos de 3 minutos**, cronometrado.

**Progreso:**

- [x] **Paso T — Contenido real cargado en producción, con criterio sobre qué era real y qué no (2026-08-04).** El usuario notó que el sitio recién desplegado estaba "desde 0" y pidió llevar lo que ya se había cargado durante el desarrollo. **No fue un volcado ciego** — antes de mover nada se revisó qué había en la base de dev y se separó lo real de lo de prueba:
  - **Real, llevado a producción:** 36 comunidades, 2 sedes, 4 centros educativos, 71 actividades/historias (las migradas del WordPress viejo en el Paso O), 8 del equipo, 1 tutoría, 1 práctica, 3 recursos, niveles, materias, 6 destinos internacionales, y sus 3316 archivos de imágenes reales (958 MB) — confirmado real, no ficticio, porque son las comunidades/instituciones reales de Coclé norte y el contenido migrado del sitio anterior, no datos de plantilla.
  - **Excluido a propósito, por instrucción explícita del usuario:** `programas` (4) y `proyectos` (4) — confirmados ficticios por el propio usuario, no por inferencia.
  - **Excluido por criterio propio, señalado antes de actuar:** `becarios` (6) — nombres como "María Fernández" duplicado con dos estados distintos y uno llamado igual que el usuario (`Fabian Conte`), patrón claro de datos de prueba, no becarios reales con consentimiento firmado (violaría el no-negociable de Fase 0 de CLAUDE.md si se publicaran). `users` (2 cuentas) — una de ellas la cuenta real del usuario pero con una contraseña que ya había quedado expuesta en el historial de git en una sesión anterior (hallazgo de la auditoría OWASP); no tenía sentido llevar a producción una cuenta con una contraseña ya filtrada. Todo lo que dependía de esos becarios/usuarios de prueba (`registros_academicos`, `horas_labor_social`, `documentos_privados`, `auditoria` de esas 6 fichas) se limpió con ellos, vía la Local API de Payload (`payload.delete({ where: {} })` por colección, respetando el control de acceso declarado — no `DELETE` de SQL a mano) para que Payload se encargara de la limpieza de tablas de locales/relaciones asociadas, no un script de SQL crudo propenso a dejar huérfanos.
  **Mecánica:** `pg_dump` de la base de dev (`--no-owner --no-privileges`, formato custom) → copiado al VPS → `pg_restore --data-only --disable-triggers` contra el Postgres de producción (que estaba vacío, sin riesgo de pisar nada real) → limpieza de lo ficticio/de-prueba vía un script de un solo uso corrido desde la misma imagen del stage `build` que ya usa `scripts/deploy.sh --migrate` (mismo patrón, reutilizado sin duplicar infraestructura) → los 958 MB de archivos de medios via `rsync` directo al volumen de Docker (`/var/lib/docker/volumes/forumpage_media_uploads/_data/`, con `chown` a la uid 1001 que usa el contenedor). El script de limpieza (`scripts/_limpiar-prod-test-data.ts`) se borró después de usarlo, tanto local como del VPS — era de un solo uso, no queda como parte del repo.
  Verificado contra el sitio real, no solo contra la base: `/api/comunidades` da 36, `/api/actividades` da 71, `/api/programas`/`/api/proyectos`/`/api/becarios` dan 0 los tres, una imagen real (`/api/media/file/...`) sirve 200 con su tamaño real, y en el navegador real las fotos cargan con sus dimensiones reales (confirmado `naturalWidth`/`naturalHeight`, no solo el `<img>` presente en el DOM). El home muestra "Becarios activos: 0" honestamente — mismo principio de visibilidad del vacío que el resto del sitio, no un número inventado.
  **Pendiente, explícito, no de esta tarea:** crear la primera cuenta real de staff en `/admin` de producción (pantalla "Create first user" de Payload, con una contraseña nueva) — hoy producción no tiene ninguna cuenta cargada.
- [x] **Paso S — Endurecimiento del VPS: `ufw`, `fail2ban` y rate limiting real en Caddy (2026-08-04).** Continuación directa del Paso Q — el usuario pidió aplicar y documentar el hardening que se había dejado como recomendación al cerrar ese paso.
  **`ufw`**: solo 22 (`limit`, no `allow` — banea temporalmente una IP con más de 6 conexiones nuevas en 30s), 80 y 443, resto denegado por defecto. **Incidente real en el camino:** un loop de polling propio (reintentando SSH cada 8s para chequear si terminaba una instalación) disparó ese mismo `limit` contra la propia IP del operador, dejando el VPS inalcanzable por SSH durante varios minutos — el sitio en sí (puerto 443) nunca dejó de responder, confirmando que el bloqueo era específico de SSH y no una caída real. Se resolvió esperando un enfriamiento largo sin más intentos (los reintentos espaciados de 30s en 30s seguían renovando la ventana). Lección documentada en `03-runbook-tecnico.md` §11.3 para no repetirla.
  **`fail2ban`**: jail `sshd` (`maxretry=5`, `findtime=10m`, `bantime=1h`) — al revisar el estado ya mostraba intentos reales de escaneo automatizado de internet contra el puerto 22 (15 fallos, 3 IPs baneadas en las primeras horas), confirmando que no es una protección teórica.
  **Rate limiting real en Caddy**, el ítem que quedaba pendiente en `05-ciberseguridad.md` §3.10 desde la auditoría OWASP (no se podía hacer sin un servidor real donde probarlo): Caddy oficial no trae rate limiting, así que se armó `Dockerfile.caddy` (build con `xcaddy` + el plugin community `github.com/mholt/caddy-ratelimit`) y `docker-compose.staging.yml` pasó de `image: caddy:2-alpine` a construir esa imagen. Nuevo bloque en el `Caddyfile`: `/api/users/*` (login, 2FA, recuperación/reset de contraseña, activación por invitación) limitado a 30 pedidos/minuto por IP — capa de red adicional al bloqueo de 5 intentos por cuenta que ya aplicaba Payload, no un reemplazo. **Validado en dos pasos, no solo desplegado a ciegas:** primero `docker build` + `caddy validate` en local contra el Caddyfile real (detectó y corrigió un error transitorio de red al descargar un módulo de Go, resuelto reintentando el build) antes de tocar el servidor en vivo; recién después desplegado con `scripts/deploy.sh` (que ya reconstruye Caddy automáticamente al tener `build:` en el compose, sin cambios al script). Verificado contra el sitio real: 31 pedidos seguidos a `/api/users/login` dan `401` (credenciales inválidas, esperado) en los primeros 30 y `429` en el 31º; el resto del sitio sigue respondiendo 200 sin degradación.
  **Ya venían resueltos de fábrica, sin que hiciera falta tocar nada:** `unattended-upgrades` (activo por defecto en la AMI de Ubuntu de AWS) y el endurecimiento de SSH (`PasswordAuthentication no`, `PermitRootLogin prohibit-password` — confirmado con `sshd -T`, no solo asumido).
  **Pendiente, no parte de esta tarea:** restringir el Security Group de AWS para que el puerto 22 solo acepte la IP del operador en vez de cualquiera (necesita esa IP, que puede cambiar) — el resto del hardening recomendado ya no lo requiere como bloqueante, es una capa extra.
- [x] **Paso Q — Primer despliegue real (VPS de AWS) + runbook y script de despliegue repetible (2026-08-04).** El usuario pidió conectar y desplegar contra un VPS real que ya tenía (AWS EC2, Ubuntu 26.04, `ubuntu@3.91.207.65`, dominio No-IP `volumetrix.servegame.com` ya apuntado). Código copiado por `rsync` (no `git clone` — el repo es privado y no valía la pena configurar credenciales de git en un servidor nuevo para esto), `.env` armado con secretos generados con `openssl rand` (nunca reutilizando los de dev), build y arranque con `docker-compose.staging.yml` ya existente desde el Paso K.
  **Bug real encontrado al migrar, no antes:** `docker compose exec app pnpm payload migrate` (el comando que documentaba `03-runbook-tecnico.md` §12.4 y que el Paso L decía "verificado") **falla contra la imagen real** — el contenedor `runner` (salida `standalone` de Next.js) no incluye el CLI de Payload, porque el *tracing* de Next.js solo empaqueta lo necesario para servir HTTP. La verificación del Paso L había corrido `payload migrate` contra un Postgres vacío desde el entorno de dev completo, nunca desde el contenedor Docker final — por eso no se había detectado antes. **Resuelto sin engordar la imagen final** (que es mínima a propósito): se construye aparte la etapa intermedia del Dockerfile (`build`, con el `node_modules` completo) como su propia imagen, y se corre `pnpm payload migrate` ahí, conectada a la misma red de Docker que `db` — nunca desde el contenedor que sirve tráfico público. Automatizado en `scripts/deploy.sh --migrate`, documentado en el nuevo [docs/runbook-despliegue.md](runbook-despliegue.md); corregida también la nota de `03-runbook-tecnico.md` §12.4 que documentaba el comando que no funciona.
  **`scripts/deploy.sh`** formaliza todo el proceso hecho a mano (`rsync` → migración opcional vía `--migrate` → `docker compose up -d --build` → estado final), parametrizado por `DEPLOY_HOST`/`DEPLOY_USER`/`DEPLOY_KEY` — nada hardcodeado al VPS actual, para que sirva igual cuando exista el droplet definitivo de DigitalOcean. Probado de punta a punta contra el VPS real (dos corridas: una completa con `--migrate` para el primer despliegue, otra sin flag para un fix posterior — Paso R de abajo).
  Verificado con el sitio real: `https://volumetrix.servegame.com/es` responde 200 con certificado real de Let's Encrypt (emitido automático por Caddy) y todas las cabeceras de seguridad (CSP con nonce, HSTS, `Permissions-Policy`); `/admin` carga; `/api/comunidades` responde JSON vacío (correcto — sin datos de seed en lo que va a ser el sitio real); las 19 migraciones acumuladas aplicaron limpio contra el Postgres del VPS. El VPS ya tenía otro proyecto (`Volumetrix`) con contenedores propios, detenidos — sin conflicto de puertos, señalado para quien lo retome.
- [x] **Paso R — Fix de hrefs en inglés del Header, desplegado al VPS real.** Continuación del hallazgo del Paso P (los 4 `href` restantes de `NAV.en` en `src/components/Header.tsx` apuntando a rutas inexistentes) — corregido en una sesión aparte (commit `8cfb96e`), revisado y mergeado a `main` como fast-forward, y desplegado al VPS del Paso Q con `scripts/deploy.sh` (sin `--migrate`, no había cambio de esquema). Verificado contra el sitio real: `/en/nosotros`, `/en/impacto`, `/en/aprende`, `/en/historias`, `/en/contacto` responden 200.
- [x] **Paso P — Páginas institucionales Programas y Contacto (2026-08-04), cierra el pendiente de "1.4 Sitio público" del Paso N.** Sin colecciones ni campos nuevos — las dos ya existían, editables por el staff desde antes (`Programas` con CRUD completo en `/staff` → Proyectos → Programas desde el Paso B; `Configuracion.contacto_institucional` editable desde `/staff` → Configuración general), solo faltaba el lado público.
  **`/nosotros/programas`** (URL anidada bajo Nosotros, no en el menú principal — así lo fija `01-documento-de-proyecto.md` §Navegación): lista los programas `activo: true`, con su color como acento visual (mismo patrón de punto de color que ya usa el filtro del Mapa de Impacto) y un conteo real de proyectos activos/completados por programa (agregado en memoria a partir de `Proyectos`, sin duplicar la consulta por programa) — un programa sin proyectos todavía muestra "Sin proyectos aún" en vez de un "0" desnudo o nada, mismo principio de visibilidad honesta del vacío que ya usan Historias e Impacto. Enlazada desde `/nosotros` con un link nuevo.
  **`/contacto`**: lee `Configuracion.contacto_institucional` (email/teléfono/dirección) por Local API con `overrideAccess: true` — la colección en sí sigue restringida a staff/directiva/admin (tiene umbrales internos que no son públicos), pero la página server-rendered solo desestructura y renderiza ese subcampo, nunca el resto del documento; `/api/globals/configuracion` sin sesión sigue dando 403 sin cambios. Si el staff todavía no cargó ningún dato de contacto, muestra un aviso en vez de una página vacía.
  **Bug preexistente encontrado y corregido de paso, no introducido por esta tarea:** el `Header` tenía los `href` en inglés de Nosotros/Impacto/Aprende/Historias/Contacto apuntando a rutas que no existen (`/about`, `/impact`, `/learn`, `/stories`, `/contact` — el proyecto usa el mismo segmento en español para ambos locales, ej. `/en/nosotros`, no `/en/about`). Se corrigió solo el de Contacto (`/contact` → `/contacto`, imprescindible para que esta página nueva fuera alcanzable en inglés); los otros cuatro quedaron con el mismo bug, señalado aparte — corregidos el 2026-08-04 en un cambio posterior (`/about` → `/nosotros`, `/impact` → `/impacto`, `/learn` → `/aprende`, `/stories` → `/historias` en `src/components/Header.tsx`), verificado sirviendo esta misma app (no la de otra sesión que ocupaba el puerto 3000): `/en/nosotros`, `/en/impacto`, `/en/aprende` y `/en/historias` responden 200 con el contenido en inglés correcto.
  Verificado con el servidor corriendo, en ambos locales: `/es/nosotros/programas` muestra los 4 programas reales de seed con sus conteos correctos (ej. "1 proyecto activo · 1 completado" para Infraestructura); `/contacto` muestra el aviso de vacío con los datos reales de dev (sin contacto cargado), y — probado temporalmente con datos reales vía script y revertido después — con datos sí muestra los tres campos con enlaces `mailto:`/`tel:` funcionando; el nuevo link "Contact" del header en inglés ya no da 404. Sin errores de consola en ninguna de las dos páginas. Build de producción real (`pnpm build`, no solo `next dev`) y `check:budget` limpios (163.1 KB / 500 KB) — las páginas nuevas no movieron la aguja del bundle porque son server components sin JS de cliente propio.
- [x] **Paso A — Payload arrancando en local.** Next.js 16 + Payload 3.82.1 + `@payloadcms/db-postgres`, Postgres local vía `docker-compose.yml` (solo DB), localización ES/EN configurada (`defaultLocale: es`) antes de crear ninguna colección, GraphQL desactivado. Colecciones `Users` y `Media` mínimas (placeholder). Verificado: `pnpm install`, `pnpm dev`, `/` y `/admin` responden 200. Ver [03-runbook-tecnico.md §2–4](../03-runbook-tecnico.md).
- [x] **Paso B — Colecciones base** (Bloque 1-2 del runbook §5): `Users` con los 4 roles + `activo` (protegidos a nivel de campo contra auto-escalación), `Media` con `imageSizes`/`consentimiento_verificado`/`contiene_menores` (con validación cruzada), `Auditoria` (solo lectura directiva/admin, escritura bloqueada desde panel), global `Configuracion`, `Comunidades`/`Sedes`/`CentrosEducativos`/`Programas`. Funciones de acceso reutilizables en `src/access/`, slug compartido en `src/fields/slug.ts`. Verificado: `tsc --noEmit` limpio, Postgres sincronizó las 23 tablas esperadas (`\dt` + columnas revisadas), `pnpm seed` cargó los 20 registros ficticios de `docs/fase-0/plantillas/` de forma idempotente (segunda corrida los detecta y omite).
- [x] **Paso C — Actividades y script de extracción de Elementor.** Colecciones `Proyectos` (con `foto_antes`/`foto_despues`, `estado`, `avance`) y `Actividades` (mural + blog unificados). `scripts/lib/elementor.ts` extrae texto e imágenes del HTML ya renderizado de Elementor (vía la API REST pública de WordPress, no scraping de HTML crudo), probado contra el sitio real. `scripts/migrate-wordpress.ts` orquesta: lee `docs/fase-0/plantillas/inventario-articulos.csv`, migra solo filas marcadas `accion=migrar`, sube imágenes a Media, crea la Actividad en locale `en` con la comunidad placeholder "Sin clasificar" (el staff reasigna la comunidad real al revisar), y emite `redirects.csv`. **Inventario real generado**: 70 artículos extraídos en vivo de forum-foundation.org (título, URL, fecha, idioma, conteo de imágenes) — cierra ese pendiente de Fase 0. Verificado con una migración real de prueba (1 artículo, revertida después): imagen descargada, Actividad creada con fecha y contenido reales, redirects.csv correcto. **Pendiente:** el staff completa la columna `accion` del inventario (varios de los 70 posts son perfiles de becario, no actividades) antes de correr `pnpm migrate:wordpress` para el resto.
- [x] **Paso D — Colecciones de Aprendizaje** (Bloque 4 del runbook §5): `Niveles`/`Materias` (taxonomías simples), `Recursos` (`fuente_y_licencia` obligatorio, `idioma` como campo propio — no localización — y campos condicionales según `tipo`), `Practicas` (tres modalidades, preguntas anidadas con opciones y retroalimentación, también condicionales según `modalidad`), `Tutorias` (materia/nivel/sede/fecha con hora/cupo/recurrencia). Verificado: `tsc --noEmit` limpio, `pnpm seed` cargó niveles, materias, 3 recursos, 1 práctica con 2 preguntas anidadas y 2 tutorías — confirmado el array anidado de preguntas/opciones en Postgres (`practicas_preguntas_locales`, `practicas_preguntas_opciones_locales`), idempotente en segunda corrida.
- [x] **Paso E — Frontend: enrutamiento ES/EN, Home y Biblioteca.** Tailwind CSS v4 con los tokens canónicos del doc 04 (no los del mockup de referencia, que tenía drift en `montana`/`cosecha` — ver análisis en la conversación). Enrutamiento `[locale]` con `src/proxy.ts` (renombrado desde `middleware.ts`, requisito de Next 16) redirigiendo `/` según `Accept-Language`. `Header`/`Footer` bilingües con selector que preserva la ruta. Home con sus 6 bloques (doc 04 §7.1) conectado a datos reales: comunidades y proyectos completados por conteo real, 3 actividades recientes, próxima tutoría — becarios activos/países muestran "—" honestamente porque `Becarios` es Fase 3, no un número inventado. Biblioteca (`/aprende/biblioteca`) con filtros por tipo/nivel/materia vía query params y paginación, conectada a `Recursos`. Se agregó `Configuracion.fecha_actualizacion_impacto` (faltaba para el bloque de cifras). Verificado con el servidor corriendo: `/`, `/es`, `/en`, `/admin` y `/es/aprende/biblioteca` (con y sin filtro) responden 200 con datos reales, sin errores en el log.
- [x] **Paso F — Frontend: mural de Historias y artículo individual.** `/historias` con filtros por comunidad/programa vía query params, rejilla de tarjetas (componente `ActividadCard` compartido con Home) y paginación. `/historias/[slug]` con encabezado fecha/comunidad/programa, contenido renderizado con `<RichText>` de `@payloadcms/richtext-lexical/react` (el serializador oficial — nunca `dangerouslySetInnerHTML`, por 05-ciberseguridad.md §3.7), galería, y enlaces a comunidad/proyecto relacionados (apuntan a fichas que aún no existen, 404 esperado hasta construirlas). `formatearFecha` extraído a `src/lib/format.ts` para no duplicar entre páginas. Verificado con el servidor corriendo: mural muestra las 5 actividades reales con filtros poblados, el artículo real (con proyecto relacionado) renderiza su contenido Lexical correctamente, sin errores de consola ni del servidor.
- [x] **Paso G — Frontend: ficha de comunidad.** `/impacto/comunidades/[slug]` con encabezado (foto, distrito/corregimiento, descripción), lista de proyectos de la comunidad con barra de avance, y mural de sus actividades recientes (reutiliza `ActividadCard`). Cierra el enlace "Ver comunidad" del artículo individual. Verificado con el servidor corriendo: navegando desde un artículo real (El Caimito) se ven sus 2 proyectos reales con estado y avance, y sus 2 actividades reales, sin errores de consola ni del servidor.
- [x] **Paso H — Frontend: slug de Proyectos y su ficha.** Agregado `slugField('titulo')` a `Proyectos` (backfill de los 3 registros existentes vía script de un solo uso, luego borrado). `/impacto/proyectos/[slug]` con estado/avance/fechas/monto, enlaces a comunidad y programa, fotos antes/después, y actividades relacionadas. Los enlaces "Ver proyecto relacionado" (artículo) y las tarjetas de proyecto (ficha de comunidad) ahora apuntan por `slug`. Verificado con el servidor corriendo: desde el artículo real de El Caimito se llega a la ficha de "Ampliación de la Biblioteca John Y. Keffer" con su monto y actividad relacionada real, y desde la ficha de comunidad ambos proyectos enlazan a sus fichas correctas.
- [x] **Paso I — Frontend: Mapa de Impacto.** `/impacto` con MapLibre GL JS (`maplibre-gl`), datos reales de `Comunidades` y `Sedes` (grupo `coordenadas` ya existente en ambas), filtro por programa a partir de `Proyectos.programa` (une comunidad→proyectos→programa), contador sobre el mapa, popup con enlace a la ficha de comunidad al hacer clic. Carga aislada del bundle de Home vía un wrapper cliente (`ImpactoMapLoader`) con `next/dynamic({ ssr: false })` — obligatorio porque MapLibre usa WebGL/`window`, no solo por el presupuesto de 500 KB (docs/spec.md línea 34). Sin `MAPTILER_KEY` en `.env` cae a las teselas demo de MapLibre (solo contornos de país, sin calles) — con la llave real en producción usa MapTiler. **Simplificación deliberada:** el panel lateral deslizable del doc 04 §7.2 se reemplazó por el `Popup` nativo de MapLibre (más simple, ya accesible); la alternativa de listas navegables que exige el doc 04 §5 ya existe vía las fichas de comunidad (Paso G). Verificado: `tsc --noEmit` limpio, `/es/impacto` responde con datos reales (9 comunidades · 3 sedes, 5 programas como filtro). **Bug real encontrado al probarlo en un navegador de verdad** (no en el pane sandboxed de esta sesión, que nunca compone frames): el mapa base cargaba pero los pines de comunidades/sedes nunca aparecían, con `Failed to load module script: ... non-JavaScript MIME type of "text/html"` en consola. Causa: `maplibre-gl` v6 calcula la URL de su worker con `import.meta.url`, que bajo el bundling de webpack de Next.js no resuelve a una ruta real → 404 servido como HTML → el worker (procesa los datos GeoJSON) nunca arranca, aunque el mapa base sí renderiza. Arreglado sirviendo una copia propia del worker desde `/public` (`postinstall` en `package.json` la copia de `node_modules/maplibre-gl/dist/`, gitignorada) y apuntando `setWorkerUrl('/maplibre-gl-worker.mjs')` antes de crear el mapa. **Segunda vuelta del mismo bug:** con logs de diagnóstico se vio que ni siquiera el evento `load` llegaba a disparar tras el primer fix — inspeccionando el propio `maplibre-gl-worker.mjs` copiado se encontró que él mismo hace `import ... from "./maplibre-gl-shared.mjs"` (ruta relativa), un segundo archivo del mismo `dist/` que no se había copiado, así que el worker fallaba al resolver su propia dependencia. `postinstall` ahora copia ambos (`maplibre-gl-worker.mjs` + `maplibre-gl-shared.mjs`) a `/public`. **Confirmado por el usuario en un navegador real:** el mapa ya pinta el basemap y los pines correctamente. Además, se quitó el programa ficticio "INADEH" de `docs/fase-0/plantillas/programas.csv` y de `scripts/seed.ts` (no era un programa real de la fundación) — el proyecto y la actividad de Chiguirí Arriba que lo usaban se reasignaron a "Infraestructura" (encaja mejor: es la construcción de un laboratorio de cómputo). **Rediseño de estructura tras referencia visual del usuario:** se mostró un mockup ("Living Impact Map") con paleta crema/terracota — rechazada explícitamente por chocar con "Por qué no crema" de `04-diseno-y-sistema-visual.md` §3.1, y con datos de becarios/arcos a universidades que son Fase 3 (`Becarios` no existe aún). Adoptada solo la **estructura**, con la paleta existente: sidebar con cifras reales (comunidades/sedes/proyectos activos, sin inventar becarios), toggle de capas (mostrar/ocultar comunidades y sedes vía `setLayoutProperty`), lista de "Comunidades de Coclé" con su avance promedio (clic hace `flyTo` + abre panel), y un **panel de detalle real** (reemplaza el `Popup` nativo — ya cumple el panel lateral del doc 04 §7.2 que antes se había simplificado) mostrando ficha resumida de la comunidad o sede seleccionada con sus proyectos individuales y enlace a la ficha completa. **Actualización tras feedback visual:** El usuario solicitó explícitamente ver los distritos de Coclé coloreados de forma sólida como en un mapa clásico. En lugar de calcular Voronoi dinámicos en el cliente (pesado), se extrajo la geometría real de OSM, se simplificó a un GeoJSON estático de 18KB y se agregó como capa de polígonos bajo los marcadores con colores pastel para coincidir con la referencia visual entregada. Se dejaron fuera deliberadamente las pestañas "Coclé core/Panamá/Global" + "Layout direction" del mockup (no aplican al modelo de datos de Fase 1). **Bug encontrado y corregido en el camino:** pasar una función (`actividadesEnComunidad`) como prop de Server a Client Component rompía con "Functions cannot be passed directly to Client Components" — se resolvió precalculando el string ya localizado en el servidor (`actividadesLabel`) en vez de mandar la función. Verificado: `tsc --noEmit` limpio, clic en una comunidad de la lista abre el panel con sus proyectos reales y el botón cerrar funciona (confirmado interactuando con el DOM directamente, ya que los clics por coordenada del pane de esta sesión no siempre llegan al elemento — limitación de la herramienta, no de la app).
- [x] **Paso J — Frontend: landing de Aprende, Tutorías y Prácticas + cifras honestas en el Mapa.** `/aprende` con los tres accesos (Biblioteca/Tutorías/Prácticas) y las próximas tutorías, conectado a `Tutorias` real. `/aprende/tutorias` con filtros por materia/nivel/sede vía query params y paginación (mismo patrón que Historias/Biblioteca). `/aprende/practicas` lista las `Practicas` publicadas con su modalidad. Se extrajo `TutoriaCard` a `src/components/TutoriaCard.tsx` (compartido entre Home, `/aprende` y `/aprende/tutorias`). El usuario preguntó por los becarios en el extranjero del mockup de referencia — se explicó que `Becarios` es Fase 3 y no existe todavía (ni la colección ni un consentimiento firmado), y se acordó cerrar primero el resto de Fase 1. Como puente honesto, el Mapa de Impacto ahora muestra las 6 cifras del doc 01 §4.3 (comunidades atendidas, obras completadas, becarios activos, sedes, proyectos activos, países alcanzados) con "—" explícito en becarios/países en vez de inventar o esconder esas categorías — visibilidad del vacío, no relleno falso. Verificado con el servidor corriendo: `tsc --noEmit` limpio, `/es/aprende` muestra las 2 tutorías reales, `/es/aprende/tutorias` filtra con materias/niveles/sedes reales poblados, `/es/aprende/practicas` muestra el quiz real, `/es/impacto` muestra las 6 cifras (4 reales + 2 con "—"), sin errores de consola.
- [x] **Paso K — Docker Compose de staging/producción.** `Dockerfile` multietapa (deps→build→runner, salida standalone, usuario no-root), `docker-compose.staging.yml` (app + Postgres sin puertos publicados + Caddy, volumen separado para `media/`), `Caddyfile` (proxy + cabeceras de seguridad + límite de subida, dominio parametrizado por `SITE_DOMAIN`), `.dockerignore`, `.env.staging.example`. Probado de punta a punta con `docker compose up` real (proyecto Docker aislado, sin tocar el Postgres ni el puerto 3000 del dev): **tres bugs reales encontrados y corregidos, ninguno visible con solo `tsc --noEmit`:**
  1. `package.json` tenía `"build": "payload build"` — ese comando no existe en el CLI de Payload 3 (nunca se había corrido `pnpm run build` en este proyecto). Corregido a `next build --webpack` (el `--webpack` porque el build por defecto usa Turbopack en Next 16, que revive el mismo panic de SCSS de `@payloadcms/ui` ya conocido en dev).
  2. Ninguna página pública tenía marcador de renderizado — Next intentaba pre-renderizarlas en build time, lo que requiere Postgres alcanzable durante `docker build` (falla en cualquier build aislado, y además está mal conceptualmente: el contenido de Payload cambia entre despliegues). Se agregó `export const dynamic = 'force-dynamic'` a las 10 páginas de `(frontend)/[locale]` que leen de Payload.
  3. El servicio `caddy` no recibía la variable `SITE_DOMAIN` — el Caddyfile quedaba con un dominio vacío y Caddy fallaba a interpretar todo el archivo ("unrecognized global option: reverse_proxy"). Agregado `environment: SITE_DOMAIN` al servicio.
  Verificado tras los tres fixes: los tres contenedores (`app`/`db`/`caddy`) levantan y quedan estables, Caddy emite certificado local y sirve HTTPS con las cabeceras de seguridad correctas (HSTS, X-Frame-Options, X-Content-Type-Options), y el proxy reenvía correctamente a la app (confirmado con `curl -k https://localhost` mostrando `via: 1.1 Caddy`).
  **Pendiente ya señalado antes:** almacenamiento en buckets separados (medios públicos / documentos privados / respaldos) del runbook §4.4 — `media_uploads` es un volumen local de Docker, un parche temporal, no el cumplimiento del no-negociable de buckets separados de `CLAUDE.md`.
  **Decisión del fundador (2026-08-04): riesgo aceptado por presupuesto.** En vez de tres buckets separados, medios/documentos/respaldos siguen en el disco del único VPS mientras no haya presupuesto para Spaces — el control de acceso a nivel de aplicación (Payload) no depende de esto y sigue siendo correcto. Compromiso explícito para no dejar la brecha de respaldo abierta del todo: los `pg_dump` se van a copiar fuera del droplet (servidor personal en casa o disco en la nube, todavía por definir) antes de considerar el respaldo real. Detalle en `05-ciberseguridad.md` §3.4.
- [x] **Paso L — Migración inicial de Payload.** `pnpm payload migrate:create initial` generó `src/migrations/20260729_052636_initial.ts` (+ snapshot `.json` + `index.ts`) a partir del esquema actual de las 14 colecciones — el proyecto nunca había tenido una migración real porque dev siempre usó `push` automático (activo por defecto fuera de `NODE_ENV=production`). Verificado con una base de datos Postgres realmente vacía (contenedor descartable, sin relación con el `db` de dev): `NODE_ENV=production payload migrate` creó las 41 tablas — mismo conteo exacto que produce el `push` de dev — y una consulta real de Payload contra esa base (`payload.find('comunidades')`) devolvió `0` resultados sin error, en vez del `relation "actividades" does not exist` que rompía el Paso K. Con esto, `docker compose exec app pnpm payload migrate` del runbook §12.4 ya tiene algo real que aplicar antes del primer despliegue. **Nota operativa hacia adelante:** cualquier cambio de esquema en una colección (campo nuevo, tipo distinto, etc.) necesita su propia migración — correr `pnpm payload migrate:create <nombre>` contra el Postgres de dev (que sí refleja el cambio, por el `push`) y commitear el resultado antes de desplegar; si no, el próximo `payload migrate` en producción no se entera del cambio.
- [x] **Paso M — CI con GitHub Actions.** `.github/workflows/ci.yml`: en cada push/PR corre `install --frozen-lockfile` → `lint` → `typecheck` → `build` → `check:budget`, con las mismas variables placeholder que usa el `Dockerfile` (no depende de un Postgres real, porque todas las páginas públicas son `force-dynamic`). Sección 14 del runbook, sin desplegar todavía a un servidor real — eso espera a que haya presupuesto para el droplet. **Bugs reales encontrados al intentar correr `pnpm lint` por primera vez en la vida del proyecto** (nunca se había corrido):
  1. No existía `eslint.config.js`/`.mjs` — ESLint 9 no tiene default de `.eslintrc.*`. Creado `eslint.config.mjs` con `eslint-config-next/core-web-vitals` + `/typescript`, ignorando `.next/`, `src/payload-types.ts` (generado), `src/migrations/**` (generado) y `public/**` (incluye los `.mjs` de MapLibre copiados por `postinstall`, no código propio).
  2. 2 errores reales de `@typescript-eslint/no-explicit-any` en `scripts/seed.ts` — el `any` ahí es deliberado (arma `data` genéricamente desde CSV contra la API de Payload), así que se desactivó la regla solo para `scripts/**/*.ts` en vez de tipar a la fuerza un script de un solo uso.
  3. Import sin usar (`Comunidad`) en la ficha de proyecto — eliminado.
  También se agregó `"typecheck": "tsc --noEmit"` a `package.json` (no existía como script, solo se corría el comando suelto) y `scripts/check-bundle-budget.mjs`, que suma el JS compartido por todas las páginas (`build-manifest.json`, comprimido gzip como reporta Next) y falla si supera 500 KB — hoy da 162.2 KB. Verificado de punta a punta con el Postgres de dev **apagado** (`docker compose stop db`) para confirmar que el build no depende de una base de datos real: `lint`, `typecheck`, `build` y `check:budget` corrieron limpios con las mismas variables placeholder que usará CI.
  **Pendiente en su momento, ya cerrado (nota corregida 2026-08-04):** el paso de despliegue real — hecho contra un VPS de AWS que ya tenía el usuario, ver Fase 1 Paso Q. El registro de imágenes versionadas de la sección 14.1 del runbook sigue sin existir (se reconstruye `latest` en cada deploy) — eso sí queda pendiente, ver [docs/runbook-despliegue.md](runbook-despliegue.md) §6.
- [x] **Paso N.5 — Mejoras en Portal Staff (Paginación, Tabs, Styling).** Se implementaron pestañas ("Becarios" y "Publicaciones") en `/staff`. Se agregó paginación real al historial de publicaciones (10 por página, con controles de Anterior/Siguiente) leyendo de Payload. Se integró el sistema visual de Forum Foundation (colores `montana`, `niebla`, tipografías `Archivo` y `Source Serif 4`) en Payload CMS mediante inyección de CSS global, y se reemplazó el logo genérico de Payload por el componente `Logo` personalizado, completando la experiencia de marca para el staff.
- [x] **Paso N — Página institucional `/nosotros` con contenido real del sitio viejo.** El usuario extrajo el WordPress anterior a `ForumOldPageInfo/` (gitignorado — volcado de referencia, no parte de la app, varios cientos de MB de imágenes) y pidió empezar por "acerca de nosotros".
  **Dos piezas nuevas, siguiendo el principio de "nada hardcodeado" incluso para una página institucional** (si el staff necesita corregir una bio o actualizar la historia, no debería depender de un desarrollador): global `Nosotros` (`mision`/`historia`, ambos richtext localizado — misión y valores son intro corta, historia es el relato largo de "Heritage", se separan en dos campos para poder darle su propia sección con encabezado) y colección `Equipo` (nombre, cargo localizado, bio, foto, `destacado` para la tarjeta más grande del fundador — igual que en el diseño original —, `orden` para el orden de aparición).
  **Contenido real cargado, no placeholders:** `scripts/seed-nosotros.ts` (uno solo, guardado como `pnpm seed:nosotros` igual que `migrate:wordpress` — reutilizable si hace falta re-sembrar en otro ambiente, con guarda de idempotencia para no duplicar) sube las 7 fotos reales del equipo a Media y carga la misión/historia/bios **traducidas al español** (el contenido original extraído está en inglés; se tradujo fielmente para el locale `es`, y se conservó el inglés original para `en` — no una traducción automática pegada sin revisar). Emparejado foto↔persona por el orden real en el HTML original de WordPress (la única ambigüedad: la foto de Janet Robinson está guardada como `Edward2.jpg` en el WordPress viejo — nombre de archivo no relacionado, pero su posición en el HTML, entre la foto de John Keffer y la de Omaira, coincide exactamente con el orden de las bios en el texto).
  **Ajuste tras feedback visual del usuario:** la columna de texto (limitada a `--text-reading-width` para legibilidad) dejaba todo el lado derecho vacío en pantallas anchas. Se agregaron dos campos más al global (`foto`, `logo` — las dos imágenes que sobraban del volcado viejo, `DSC06238-scaled.jpg` y `Foundation-Logo-2-2015.png`, ambas staff-editables como el resto) y se pasó la sección de misión/historia a una grilla de dos columnas: texto a la izquierda, foto+logo en una columna `sticky` a la derecha (se queda a la vista mientras se lee la historia larga, en vez de desaparecer scroll abajo). En mobile colapsa a una sola columna, con la foto/logo aterrizando naturalmente después de la historia y antes del equipo — verificado en ambos anchos.
  Verificado con el servidor corriendo en ambos locales: `/es/nosotros` y `/en/nosotros` muestran la misión, la historia completa, la foto+logo ya no dejan el lado derecho vacío, y los 7 miembros del equipo con sus fotos reales; la tarjeta de John Keffer aparece más grande (`destacado`), confirmado en el DOM; sin errores de consola en desktop ni mobile. `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB). Migraciones generadas y verificadas contra un Postgres vacío (15 acumuladas).
  **Pendiente de "1.4 Sitio público" en su momento, ya cerrado (nota corregida 2026-08-04):** páginas institucionales de Programas y Contacto — construidas en el Paso P, arriba.
- [x] **Paso O — Migración completa de las 70 historias comunitarias de `ForumOldPageInfo/historias/`.** Se creó y ejecutó el script de migración masiva `scripts/migrate-all-historias.ts` (ejecutable vía `pnpm migrate:historias`), el cual procesó automáticamente las 70 carpetas de artículos extraídos del WordPress anterior.
  **Logros clave:**
  1. **Preservación de Fechas Reales de Publicación:** Se extrajo la fecha original del metadato (`- **Fecha:** ...`) de cada `articulo.md` (ej. 2020, 2021, 2022, 2023) asignándola a `fecha_publicacion` en formato ISO, en lugar de usar la fecha actual del sistema.
  2. **Traducción y Bilingüismo (es/en):** Se configuraron los campos localizados de `titulo`, `extracto` y `contenido` (en RichText Lexical) para ambos locales (`es`/`en`).
  3. **Procesamiento de Medios:** Subida de portadas e imágenes de galería a la colección `Media`, reutilizando archivos existentes vía caché en memoria para optimizar el rendimiento.
  4. **Resolución Inteligente de Comunidad:** Asociación automática de cada historia con su comunidad correspondiente en Coclé Norte (Túrega, Caimito, Machuca, Río Indio, etc.) mediante coincidencia de palabras clave.
  Verificado con `pnpm migrate:historias` (70/70 procesadas) y `tsc --noEmit` limpio (0 errores).
  **Corrección posterior, tras feedback real del usuario viendo los artículos publicados** ("hay imágenes repetidas, no está clasificado bien, la portada no se ve bien"): las tres quejas eran síntomas de bugs concretos en `migrate-all-historias.ts`, no apreciaciones subjetivas.
  1. **Imágenes repetidas — causa raíz real, no supuesta:** WordPress reexportó la misma foto varias veces bajo nombres distintos por artículo. La primera limpieza deduplicó por **hash MD5** del contenido (limpiando las que eran copias idénticas byte por byte). Sin embargo, seguían existiendo duplicados *visuales* porque WordPress también generó versiones redimensionadas o comprimidas de la misma foto (distinto tamaño de archivo, distinto hash MD5, misma imagen visual). **Fix final:** se creó `scripts/clean-visual-duplicates.ts` utilizando Perceptual Hashing (pHash) vía `sharp` (escala a 8x8 px, escala de grises, y comparación de distancia de Hamming). Este fix limpió 64 repeticiones visuales adicionales en 46 publicaciones, dejando finalmente una sola imagen (portada) y sin repeticiones en la galería.
  2. **Clasificación de comunidad:** la lista de palabras clave a mano tenía huecos reales (le faltaban Coclesito, El Harino, La Pintada y Tulú — 4 de las 9 comunidades reales de la base — y en cambio incluía "penonome"/"san miguel", que no son nombres de comunidades sino de distrito/centro educativo). Reemplazada por comparación directa contra los nombres de comunidad que ya existen en la base (sin tildes) — se detecta sola cualquier comunidad nueva, sin mantenimiento a mano. Igual de importante: **el `programa` se asignaba siempre al primero de la lista** (`programas[0]`, "Tutorías") sin ninguna relación con el contenido real — 71 de 75 artículos quedaban etiquetados "Tutorías" sin importar de qué trataran. A diferencia de comunidad, no hay una heurística de texto confiable para programa, así que se dejó **sin asignar** en vez de forzar una etiqueta incorrecta — el staff lo asigna al revisar, mismo criterio que la comunidad "Sin clasificar" (que sigue existiendo y es correcta: 45/70 artículos, en su mayoría perfiles personales que nunca mencionan una comunidad por nombre, no tienen de dónde inferirla).
  3. **Portada "no se ve bien":** con la corrección anterior de recorte de caras, la portada pasó a renderizarse a ancho completo sin ningún límite de altura — para una foto vertical de celular (confirmado un caso real: 960×1280px), eso da una portada de **~1700px de alto** en una pantalla normal, empujando todo el contenido fuera de la vista. Se agregó un tope de altura (`max-h-[520px]`) manteniendo `object-cover` con `object-top` (prioriza la parte de arriba del recorte, donde suele estar la cara) — funciona igual de bien para fotos horizontales (rara vez llegan al tope) y verticales (quedan a una altura razonable, sin recortar la cara).
  **Bug adicional encontrado depurando el fix #1, no reportado por el usuario pero real:** la primera versión del fix pasaba `galeria: undefined` cuando la galería deduplicada quedaba vacía — en un `update` de Payload, un valor `undefined` significa "no toques este campo", así que una galería que debía quedar vacía **nunca se vaciaba**, conservando en silencio los IDs viejos de antes del fix. Detectado porque el navegador seguía mostrando 3 imágenes en un artículo que ya debía tener 0 — se corrigió pasando el array vacío explícito (`galeria: galeriaIds`, nunca `undefined`) y lo mismo para `portada` (`portadaId ?? null`).
  Verificado de punta a punta tras el fix: `pnpm migrate:historias` reprocesó las 70 (0 creadas, 70 actualizadas); auditoría automática confirmó 0 artículos con portada duplicada en su galería; distribución de comunidad ahora cubre Coclesito/La Pintada/Toabré/Tulú/Río Indio/Chiguirí Arriba/El Caimito además de "Sin clasificar"; los 70 artículos migrados quedaron sin programa asignado (en vez de 71/75 forzados a "Tutorías"); confirmado en el navegador real que un artículo con una sola foto real ya no repite nada (1 sola imagen en toda la página) y uno con 6 fotos reales distintas las muestra todas sin solapamiento; altura de portada vertical confirmada en 520px (antes ~1700px) vía `clientHeight` real del DOM. `tsc`/lint/build/`check:budget` limpios. Se eliminaron tres scripts de diagnóstico sin terminar (`check-nitzia.ts`, `clean-duplicate-gallery.ts` — comparaba por ID exacto, nunca iba a detectar el problema real; `clean-visual-duplicates.ts` — hash perceptual, más complejo de lo necesario dado que la causa real era duplicado exacto de bytes) — quedaron sin commitear y innecesarios una vez arreglada la causa en el propio script de migración.
  **Nota para quien retome:** cada re-corrida de `migrate:historias` sube archivos nuevos a Media aunque el contenido sea idéntico al de una corrida anterior (el caché de deduplicación es en memoria, dura solo un proceso) — quedaron algunos Media huérfanos de las corridas previas a este fix, sin impacto funcional (nada los referencia) pero ocupan espacio; limpiarlos es opcional y no se hizo en este paso.
  **Segunda vuelta, tras ver el recorte de `object-top` en fotos reales publicadas:** un solo punto de recorte fijo para las 70 fotos no podía funcionar — para una foto de retrato "arriba" suele ser la cara, pero para una foto de evento (banner de graduación, gente sentada en una mesa) puede cortar justo lo importante. El staff necesita poder ajustar esto **sin tocar código**. Antes de construir nada propio: Payload ya trae esto integrado (`upload.focalPoint`, activo por defecto, nunca desactivado en `Media.ts`) — el botón **"Edit Image" en cualquier documento de Media** abre un editor con recorte + un punto focal arrastrable sobre la foto, que graba `focalX`/`focalY` (0-100) en el propio documento. Solo faltaba que el frontend **leyera** ese valor — antes lo ignoraba por completo y usaba el `object-top` fijo. Se cambió a `style={{ objectPosition: `${focalX ?? 50}% ${focalY ?? 50}%` }}` en portada y en las miniaturas de galería (default 50/50 = centro si el staff nunca lo toca, mejor punto de partida general que "arriba" fijo).
  **Cómo lo usa el staff:** entrar a Media → abrir la foto → "Edit Image" → arrastrar el punto sobre la cara o lo que importa → Apply Changes → Save en el documento. Ningún cambio de código, ni siquiera reingresar al artículo — el punto vive en la imagen, no en el artículo, así que sirve para donde sea que esa foto se use.
  Verificado con una foto real de las quejadas por el usuario ("Following Family Footsteps", el graduando con el cartel): sin ajustar, el recorte fijo (`object-top`) mostraba solo el borde superior del cartel; con el punto focal movido a 44/40 vía el editor real de Payload (no simulado — sesión de admin real, botón real, guardado real), el artículo real muestra ahora la cara, el birrete y el diploma completos, confirmado con el `objectPosition` computado del DOM (`44% 40%`) y por captura visual.
  **Documentado en [docs/manual-staff.md](manual-staff.md) (creado 2026-08-04):** este flujo de "Edit Image" y el resto de las funciones del panel, en texto — sin capturas, para no atarlo a como se vea la interfaz hoy.
- [ ] Resto del checklist de infraestructura, colecciones, frontend y mapa: ver [03-runbook-tecnico.md](../03-runbook-tecnico.md)

> Nota de entorno: en esta máquina el dev server de Next.js con **Turbopack** produce un panic intermitente compilando el CSS del panel admin de Payload (`@payloadcms/ui`). Se fijó `next dev --webpack` en `package.json` como mitigación — ver commit correspondiente.

### Fase 2 — Centro de Aprendizaje

Biblioteca, recursos con licencia obligatoria, prácticas en sus 3 modalidades, videos de YouTube diferidos, progreso guardado en el dispositivo.

**Progreso:**

- [x] **Paso F — Verificación: el staff crea una práctica sin asistencia.** Último pendiente del checklist de Fase 2 (`02-plan-de-ejecucion.md` §Fase 2, criterio "el staff crea un quiz de cinco preguntas sin asistencia"): confirmar que el formulario ya generado automáticamente por Payload para `Practicas` (sin código de UI propio) es realmente usable de punta a punta, no solo que el schema "se vea bien". Verificado en el navegador real, actuando como el staff: se creó el primer usuario (pantalla "Create first user" de Payload, rol `staff` por defecto), y desde `/admin/collections/practicas/create` se armó un quiz real de 5 preguntas de geografía de Coclé — título, modalidad (react-select), 5 bloques de "Pregunta" (botón "Add Pregunta"), cada uno con sus opciones (botón "Add Opcione"), índice de respuesta correcta y retroalimentación — guardado sin errores, con el `slug` autogenerado correctamente (`quiz-de-geografia-de-cocle`) por el hook ya construido en Fase 1. Se confirmó que el quiz recién creado por "el staff" funciona en `/aprende/practicas/[slug]` igual que el de prueba del Paso A, y que `respuesta_correcta`/`retroalimentacion` siguen sin aparecer en `/api/practicas` para este registro real también. Usuario y práctica de prueba borrados después (decisión del usuario: no dejar datos de prueba en dev). **Con esto, Fase 2 — Centro de Aprendizaje queda cerrada.**
- [x] **Paso E — Sección de inglés con enfoque pedagógico.** `01-documento-de-proyecto.md` §11 distingue el bilingüismo institucional (selector ES/EN, traducción de páginas) del inglés como *contenido* del Centro de Aprendizaje — "prioridad declarada de la fundación", no traducción. En `/aprende` se agregó una cuarta tarjeta "Inglés" (borde en cosecha, distinta de las otras tres) que enlaza a `/aprende/biblioteca?materia={id}` — reutiliza el filtro de materia ya construido en el Paso B, sin duplicar lógica ni crear una página nueva. El `id` se resuelve buscando la materia de nombre "Inglés" en Payload (no un ID fijo en código: si el staff aún no la ha creado, la tarjeta simplemente no aparece — visibilidad honesta del vacío, mismo patrón que las cifras "—" del Mapa de Impacto). Verificado con el servidor corriendo en ambos locales: `/es/aprende` y `/en/aprende` muestran la tarjeta con su copy correspondiente, el enlace resuelve a `?materia=3` (el id real de "Inglés" en la base de dev), y `/aprende/biblioteca?materia=3` llega prefiltrado mostrando solo el recurso real de Duolingo con el select de materia ya en "Inglés" — sin errores de consola.
- [x] **Paso D — Descarga de materiales para uso sin conexión.** `enlace_externo` y `video_youtube` son por naturaleza contenido en línea (un enlace externo o un video de YouTube no se pueden "descargar para usar sin conexión" de forma honesta ni legal) — la única categoría de Biblioteca que puede cumplir el criterio de verdad es `pdf_propio`, autohospedado en Media. Antes, su tarjeta abría el PDF en una pestaña nueva (visor del navegador, sin garantía de que quedara guardado en el dispositivo). Ahora el enlace lleva `download` (fuerza guardado real del archivo, sin `target="_blank"`) y una etiqueta explícita "↓ Descargar para uso sin conexión"; `enlace_externo` muestra en cambio "Abrir enlace ↗", dejando claro cuál de los dos promete funcionar sin conexión y cuál no — visibilidad honesta de la limitación, no una promesa de offline que no se puede cumplir para contenido externo. Verificado con el servidor corriendo: se creó un `Recurso` `pdf_propio` real de prueba (con su `Media` real) y se confirmó en el DOM que su `<a>` trae `download` (sin `target`) y el texto correcto; el recurso `enlace_externo` real ya existente sigue abriendo en pestaña nueva con "Abrir enlace ↗". Datos y archivo de prueba borrados después.
- [x] **Paso C — Videos de YouTube con carga diferida.** `src/lib/youtube.ts` extrae el ID de video de las formas comunes de URL (`watch?v=`, `youtu.be/`, `/embed/`, `/shorts/`). Nuevo `VideoYoutube` (cliente): muestra solo una miniatura estática (`i.ytimg.com/vi/{id}/hqdefault.jpg`) con botón de reproducción hasta que el usuario hace clic — recién ahí monta el `<iframe>` de `youtube-nocookie.com` (dominio sin cookies de seguimiento antes de reproducir, acorde al criterio de Fase 2 de no recolectar datos personales). En Biblioteca, `RecursoCard` para `tipo: video_youtube` deja de ser un `<a>` que abre YouTube en pestaña nueva y en su lugar incrusta el reproductor directamente en la tarjeta. Verificado con el servidor corriendo: `document.querySelectorAll('iframe').length` da `0` en la carga inicial de `/aprende/biblioteca` (nada de YouTube se pide todavía) y pasa a 1 con el `src` correcto de `youtube-nocookie.com/embed/...` recién después de simular el clic en el botón de reproducción.
- [x] **Paso B — Filtros de Biblioteca sin recarga de página.** El `<form method="get">` de tipo/nivel/materia disparaba una navegación dura del navegador en cada submit (igual que un `<a href="?...">` normal en la paginación). Se reemplazó por `FiltrosBiblioteca` (nuevo componente cliente en `src/components/`), que aplica cada filtro al cambiar el select vía `router.push` de Next — misma URL con query params, pero solo se reemplaza el payload de la ruta, sin recarga completa del documento. La paginación pasó de `<a>` a `<Link>` por la misma razón. La página en sí sigue siendo Server Component con la misma consulta a Payload de siempre (sin duplicar lógica en un endpoint nuevo). Verificado con el servidor corriendo: se marcó `window.__marcaSinRecarga` antes de cambiar el filtro de materia a "Inglés" — la URL cambió a `?materia=3`, el resultado se filtró correctamente a 1 recurso real, y la marca en `window` seguía presente después (prueba de que no hubo recarga completa); revertir a "Todos" restauró los 3 recursos, sin errores en consola.
- [x] **Paso A — Quiz interactivo en Prácticas.** `Practicas` ya tenía el esquema completo desde Fase 1 (Paso D); faltaba la experiencia real de tomar el quiz. Se agregó `slugField('titulo')` a la colección (con backfill del único registro existente). Nueva `/aprende/practicas/[slug]`: modalidad `descargable` muestra el botón de descarga (reutiliza `archivo.url` de Media, mismo patrón que Biblioteca); `quiz_autocorregido`/`quiz_con_progreso` renderizan `<QuizPractica>` (cliente). **Decisión de seguridad no trivial:** calificar en el navegador habría obligado a mandar `respuesta_correcta` al cliente desde el primer render — visible en el HTML/RSC payload antes de responder nada. En vez de eso, se protegieron `respuesta_correcta` y `retroalimentacion` con field-access (`esStaffOSuperiorFieldAccess`, nuevo en `src/access/index.ts`) para que ni siquiera `/api/practicas` público las exponga (verificado con `curl` directo al endpoint, no solo mirando la interfaz — como exige CLAUDE.md), y se creó `src/actions/calificar-practica.ts` (`'use server'`, con `overrideAccess: true` de servidor) que recibe las respuestas del alumno y devuelve solo `{correcto, retroalimentacion}` por pregunta más el puntaje — nunca el índice correcto en sí. `quiz_con_progreso` además guarda `{aciertos, total, fecha}` en `localStorage` tras calificar (lectura diferida a un `useEffect` post-montaje para no romper la hidratación SSR/cliente) y muestra "Completado antes: X/Y" en visitas siguientes; `quiz_autocorregido` no persiste nada. Verificado con el servidor corriendo, contra un registro real: primer intento con una respuesta correcta y una incorrecta mostró ✓/✗ y retroalimentación reales, "Reintentar" limpia el estado; con la modalidad cambiada temporalmente a `quiz_con_progreso` (revertido después de probar) se confirmó el guardado y la lectura de `localStorage` tras recargar, sin errores de hidratación en consola.

### Fase 3 — Portal del Becario

Autenticación + 2FA, expediente académico con automatismo de suspensión/reactivación, labor social, desembolsos, pipeline de Necesidades.

**Progreso:**

- [x] **Alta de becario unificada: expediente + cuenta + invitación en un solo paso de `/staff` (2026-08-05).** El usuario pidió unificar los dos pasos que existían hasta ahora: "Registrar Becario" en `/staff` (solo creaba el expediente) y después ir a `/admin` a mano para crear el `User` con rol `becario`, vincularlo, y copiar el `enlace_invitacion` generado. Sin colección ni hook nuevo — `Users.ts` ya tenía todo lo necesario (`create: esStaffOSuperior` en la colección, y el hook `afterChange` `generarInvitacionAlCrear` que dispara con cualquier `payload.create` sobre `users` con `rol: 'becario'`, sin importar si viene del admin nativo o de un server action). Solo hacía falta juntar ambos `payload.create` en la misma acción.
  **`src/actions/crear-becario.ts`**: agregado el campo `email` al input (validado con una regex simple, sin dependencia nueva). Tras crear el `Becarios`, crea el `Users` correspondiente (`rol: 'becario'`, `becario: <id nuevo>`, contraseña de relleno de 32 bytes aleatorios — el hook la pisa enseguida, igual que ya hacía el flujo de `/admin`) y relee el documento para devolver `enlace_invitacion` al cliente. **Detalle no obvio, encontrado antes de escribirlo mal:** el `doc` que devuelve `payload.create()` no trae el enlace todavía — el hook lo escribe con un `update` posterior, async, dentro de su propio `afterChange`; hace falta un `payload.find` aparte después del `create` para releerlo. Ese `find` necesita `overrideAccess: true` a propósito: el `access.read` de `users` a nivel de colección limita a un staff (no-admin) a ver solo su propio documento (`id: { equals: req.user.id }`), así que con `overrideAccess: false` la relectura del becario recién creado volvería vacía para cualquier staff real. Seguro igual: `sesionActual()` ya valida arriba que quien llama es staff/admin, y la acción nunca devuelve al cliente nada del documento `users` salvo el string del enlace. Si crear la cuenta falla (email duplicado, etc.) el expediente igual queda guardado — no se revierte — y se avisa en el formulario para reintentar desde `/admin` en ese caso puntual.
  **`FormularioNuevoBecario.tsx`**: nuevo campo "Correo del becario" (obligatorio) junto al de nombre. Al guardar, el modal ya no se cierra solo — muestra un panel de éxito con el enlace en un input de solo lectura y un botón "Copiar" (`navigator.clipboard`), porque el enlace es de un solo uso y esta es la única vez que se ve.
  **Verificado con un script de Local API** (réplica exacta de la lógica del action, no una suposición): becario y cuenta creados, `enlace_invitacion` devuelto con la forma real (`/es/cuenta/activar?token=...`), datos de prueba borrados después. **La verificación en el navegador real quedó incompleta por una limitación ya documentada de esta sesión** ("los clics por coordenada del pane de esta sesión no siempre llegan al elemento") — se confirmó con un control (el botón preexistente "Cerrar sesión" tampoco respondió al clic vía la herramienta) que el problema es de la herramienta, no del código nuevo; no se pudo compensar interactuando por DOM directo esta vez porque el propio botón "Menú"/estado de React no reaccionaba a ningún método de disparo de evento probado. `tsc --noEmit` y `eslint` limpios.
  **Pendiente real, no de esta tarea:** confirmar visualmente en un navegador real (fuera de esta sesión) que el modal de éxito se ve y el botón "Copiar" funciona — la lógica de negocio está probada, la UI de React no se vio renderizada en pantalla.
- [x] **Content-Security-Policy, probada en staging (2026-08-04) — cierra el único pendiente que quedaba de la auditoría OWASP.** La recomendación original (ver más abajo) asumía que la CSP viviría en el `Caddyfile`, con el riesgo de dejar `/admin` inaccesible si Payload necesitaba `'unsafe-inline'`/`'unsafe-eval'` para su JS. En la práctica se armó distinto y sin ese riesgo: Next.js 13.4+ soporta un patrón de **nonce por request generado en middleware**, que el propio framework detecta solo desde el header `Content-Security-Policy` de salida (`getScriptNonceFromHeader`) y aplica automáticamente a sus scripts (streaming RSC + chunks estáticos) — no hace falta tocar `layout.tsx` ni el `Caddyfile`. Implementado en `src/proxy.ts` (donde ya vivía el redirect de idioma — Next 16 renombró `middleware.ts` a `proxy.ts`; un `middleware.ts` nuevo aparte causaba el error "Both middleware file and proxy file are detected" y devolvía 404 en todo el sitio, detectado de inmediato al probar).
  `script-src` usa `'nonce-<random>' 'strict-dynamic'` (sin depender de `'self'`, que `strict-dynamic` ignora). `style-src` lleva `'unsafe-inline'` aparte y a propósito: los nonces de CSP no cubren atributos `style=""` inline (solo `<style>`/`<link>`), y el sitio (11 archivos) más el admin de Payload usan `style={{}}` — no se justificaba migrar eso a CSS por sacar un directive de bajo riesgo (no ejecuta JS). Dominios externos reales del sitio quedaron explícitamente permitidos, no un wildcard: `api.maptiler.com` y `{a,b,c}.basemaps.cartocdn.com` en `connect-src` (tiles del mapa, pedidos por `fetch`/XHR de `maplibre-gl`, no `img-src`), `i.ytimg.com` en `img-src` (miniatura de YouTube antes del clic) y `youtube-nocookie.com` en `frame-src` (el iframe una vez reproducido). `worker-src 'self'` va explícito, no como fallback de `script-src` — con `strict-dynamic` ese fallback habría dejado sin permiso al worker propio de `maplibre-gl` (`/maplibre-gl-worker.mjs`).
  `/admin` estaba excluido del matcher original de `proxy.ts` (correcto para el redirect de idioma, que no le aplica) — se amplió el matcher para que también reciba la CSP, con un branch aparte que la aplica sin intentar redirigirlo.
  **Verificado, no asumido:** build de producción real (`pnpm build` + `node .next/standalone/server.js` con `NODE_ENV=production`, mismo método que la verificación de la actualización de Payload) — home, `/admin` (login), `/portal` (login), `/es/impacto` (mapa completo: tab, tiles, worker, canvas montado) y `/es/aprende/biblioteca` (clic real en miniatura → iframe de YouTube) cargan sin un solo error de consola. Cada directiva se probó además de forma positiva **y** negativa con el evento nativo `securitypolicyviolation` del navegador (más confiable que leer texto de consola): `fetch()` a `maptiler`/`cartocdn` permitido, a un dominio inventado bloqueado; `<img>` a `i.ytimg.com` permitido; `<iframe>` a `youtube-nocookie.com` permitido, a un dominio inventado bloqueado (`violatedDirective: "frame-src"`); `new Worker('/maplibre-gl-worker.mjs')` construido sin violación. `tsc --noEmit` limpio.
- [x] **Manual del becario (2026-08-04).** El usuario notó que solo existía `docs/manual-staff.md` — no había nada equivalente para la población más numerosa del sitio. Nuevo `docs/manual-becario.md`: activación de cuenta por invitación, login (incluido el flujo de "olvidé mi contraseña", que ya existe y respeta 2FA si está activo), qué se ve en `/portal` (estado de la beca, progreso de labor social, reportes, desembolsos), cómo reportar horas con evidencia (requisitos reales del formulario, no inventados), y Seguridad de mi cuenta. **Aclarado explícitamente que el becario no sube su propio expediente académico** — `crearRegistroAcademico` solo se usa desde `FormularioNuevoAcademico.tsx` en `components/staff/`, nunca desde el Portal; confirmado por grep antes de escribirlo, no asumido. Cross-linkeado con `manual-staff.md`.

- [x] **Actualización de Payload 3.82.1 → 3.87.0 — cierra el pendiente de mayor severidad de la auditoría (2026-08-04).** Antes de tocar nada se revisaron las notas de release de las 8 versiones intermedias buscando "breaking"/"migration" (ninguna encontrada) y se confirmó que `v3.83.0` es la que trae el fix real (`drizzle-orm` bumpeado a `0.45.2` "to resolve an SQL injection vulnerability" — exactamente el hallazgo pendiente de la auditoría de abajo). Una mención a "remove deprecated root endpoint property" en v3.87.0 preocupó en un primer momento (este proyecto usa `endpoints` custom extensivamente en `Users.ts`) pero resultó ser un cambio de solo documentación al revisar el commit real.
  **Verificado de punta a punta, no solo con `tsc` limpio:** los 4 endpoints custom de autenticación probados contra el servidor real — login en dos pasos con 2FA completo (código TOTP calculado con el mismo algoritmo RFC 6238, cookie de sesión real emitida y confirmada válida), política de contraseña fuerte de la auditoría (rechaza 3 caracteres), bloqueo por fuerza bruta (5 intentos → cuenta bloqueada) — los puntos de mayor riesgo de una actualización de framework, todos intactos. Matriz de acceso anónimo repetida contra las 24 colecciones, resultado idéntico a antes de actualizar. `pnpm audit`: `drizzle-orm` (SQL injection alto) y `uuid` (moderado) ya no aparecen, de 17 avisos a 14 (los que quedan siguen pinneados por la propia dependencia interna de Payload). Estado de migraciones sin cambios (19/19, el fix es interno a Drizzle, no afecta el esquema). `tsc`/`eslint`/`pnpm build`/`check:budget` (163.1 KB) limpios.
  Detalle completo en [docs/security-audit-2026-08-04.md](security-audit-2026-08-04.md), sección "Actualización de Payload 3.82.1 → 3.87.0".

- [x] **Auditoría de seguridad OWASP Top 10 antes de publicar (2026-08-04).** Reporte completo en [docs/security-audit-2026-08-04.md](security-audit-2026-08-04.md) — herramientas instaladas y corridas (semgrep, gitleaks, pnpm audit) + revisión manual de las 24 colecciones/2 globals cruzada contra `01-documento-de-proyecto.md`, probada en vivo con `curl`, nunca solo contra la interfaz. **Veredicto: apto para publicar**, sin vulnerabilidades críticas ni de severidad alta explotables en el código propio.
  **9 cambios aplicados el mismo día**, cada uno verificado antes/después: dos fugas reales de acceso a nivel de campo (`Necesidades.costo_estimado` y `Becarios.condicion_socioeconomica_verificada` visibles a cualquier anónimo — corregidas con el mismo patrón de field-access ya usado en el resto del código), un mínimo de contraseña inconsistente (Payload por defecto exige solo 3 caracteres; el flujo de invitación de becarios —el que usa toda la población de becarios— no tenía el mínimo de 8 que sí exige "Cambiar contraseña"; corregido con un hook `beforeValidate` en `Users.ts`), tres endurecimientos de configuración (`poweredByHeader: false`, `Permissions-Policy` restrictiva, `X-Powered-By` fuera), y actualización de dependencias (`next` 16.2.7→16.2.12 corrige 4 CVEs altos propios de Next.js, `sharp` 0.34.2→0.35.3 corrige 4 CVEs altos de libvips, overrides de `postcss`/`dompurify` de solo build/admin).
  **Un intento de fix revertido en el momento:** forzar `brace-expansion` a una versión parcheada rompió ESLint (el paquete publica dos líneas de versión mayor incompatibles bajo el mismo nombre) — revertido de inmediato, es deuda de solo lint sin exposición en producción, documentado en `pnpm-workspace.yaml` para que nadie reintente el mismo override.
  **Pendiente original de esta entrada, cerrado el mismo día — ver las dos entradas de arriba:** actualizar Payload (cierra el SQL injection) y armar la Content-Security-Policy, probada en staging antes de tocar producción, tal como se recomendaba acá.
  **Bug de tooling encontrado en el camino, no de la app:** un comentario SQL con backticks de Markdown dentro de un template literal de TypeScript rompía el parseo de una migración con esbuild — ya corregido en una entrada anterior de este mismo día, mencionado acá porque el patrón es el mismo tipo de hallazgo que esta auditoría busca (cosas que solo aparecen probando de verdad, no leyendo el código).

- [x] **Tres pendientes de deuda técnica cerrados a pedido del usuario — "arregla todo, dejalo listo" (2026-08-04).** El usuario pidió cerrar todo lo que quedaba abierto en `docs/plan.md` con código (excluyendo Fase 0, que no depende de código). Tres pendientes reales, cada uno verificado antes de pasar al siguiente:
  1. **Destinos Internacionales Frecuentes hardcodeados** (`FormularioNuevoBecario.tsx`/`FormularioEditarBecario.tsx` tenían un `if/else` fijo con 6 universidades — violaba "nada hardcodeado" de CLAUDE.md en espíritu: el staff no podía agregar un séptimo destino sin tocar código). Nueva colección `DestinosInternacionales` (`universidad`/`pais`/`ciudad`/`coordenadas`/`bandera`, editable por staff, lectura pública), sembrada con las mismas 6 universidades que ya estaban hardcodeadas (Bocconi, University of Florida, Navarra, Tecnológico de Monterrey, EARTH, Zamorano) para no perder el dato real. Los dos formularios ahora arman el `<select>` desde la lista real, vía un prop nuevo (`destinos`) que pasa por `TablaBecarios.tsx` → `staff/page.tsx` y `[becarioId]/page.tsx`. Verificado en el navegador logueado como staff (sesión real del usuario, sin mutar nada — se abrió el modal, se confirmó que las 6 opciones vienen de la base con su bandera y orden alfabético, y se cerró sin guardar).
  2. **No había visor de `Auditoria` en el panel** — colección con `access.read: esDirectivaOSuperior` que ya se poblaba sola desde varios hooks (`suspension_automatica`, `verificacion_registro_academico`, etc.) pero nadie sin acceso a `/admin` podía leerla. Nueva página `/[locale]/directiva/auditoria` (mismo patrón de gating que `/directiva/necesidades`, con un link cruzado entre ambas para que se descubran) — tabla paginada (30 por página) con actor/acción/colección/documento y un `<details>` nativo (sin JS extra) para ver `valor_anterior`/`valor_nuevo`. **Nota de alcance:** vive en `/directiva`, no en `/staff` — `Auditoria.access.read` excluye a `staff`, así que meterlo en `/staff` le habría dado a staff acceso a algo que la colección explícitamente le niega. Verificado con una cuenta directiva descartable vía `curl` + cookie-jar aislado (nunca el navegador compartido, por la lección de la sesión anterior): eventos reales visibles con sus valores, `/api/auditoria` da 403 sin sesión y 200 con sesión directiva. Cuenta de prueba borrada después.
  3. **`Becarios.foto` seguía pública para siempre aunque el becario revocara su consentimiento** (`mostrar_en_mapa: false`) — hueco señalado desde la entrada de "Remediación de privacidad de documentos" de más abajo, sin resolver hasta ahora. Nueva colección `FotosBecarios` (upload, con un campo `publica` de solo lectura) con `access.read` condicional: pública para anónimos solo si `publica: true`, siempre visible para staff/directiva/admin. Un hook `afterChange` nuevo en `Becarios` (`sincronizarFotoPublica`) mantiene `publica` igual a `mostrar_en_mapa` en cada save, así el staff nunca tiene que acordarse de tocar dos colecciones a mano. `Becarios.foto` pasó de `relationTo: 'media'` a `relationTo: 'fotos-becarios'`, con una migración que copia cualquier foto ya referenciada preservando su `id` (mismo patrón que la migración de `DocumentosPrivados`, incluida la copia del sidecar de `alt` localizado) y calculando `publica` en la propia copia a partir del `mostrar_en_mapa` vigente — hoy esa copia fue un no-op real (0 becarios tenían `foto` seteada: el campo nunca estuvo expuesto en ningún formulario, solo existía en el schema). Verificado de punta a punta vía script + `curl`: becario de prueba con `mostrar_en_mapa: true` → foto sirve 200 anónimo; al revocar el consentimiento → la foto pasa a `publica: false` sola y el archivo da 403, coincidiendo con que `/api/becarios/<id>` ya daba 404 para ese mismo caso. Esto resuelve en código, sin esperar a un droplet/bucket, el pendiente que la entrada de abajo dejaba atado a "buckets separados + URL firmada" — el control de acceso declarativo de Payload (igual que ya protege `DocumentosPrivados`) alcanza para un caso condicional como este.
  **Bug propio encontrado y corregido en el camino:** el `.gitignore` no tenía la carpeta de subidas `fotos-becarios/` (se agregó antes de que hubiera ningún archivo real adentro — la regla que CLAUDE.md pide desde el primer commit de cada carpeta de uploads). De paso se encontró que `documentos-privados/` **tampoco** estaba en los volúmenes de `docker-compose.staging.yml` desde que se creó esa colección — un despliegue real habría perdido esos archivos en cada redeploy porque solo `media_uploads` tenía volumen persistente. Se agregaron `documentos_privados_uploads` y `fotos_becarios_uploads` al compose de staging.
  `tsc --noEmit`, `eslint` y `pnpm build` limpios; `check:budget` sigue en 162.2 KB / 500 KB. Migraciones (`agregar_destinos_internacionales`, `agregar_fotos_becarios`) verificadas contra el Postgres de dev con `down()` incluyendo `IF EXISTS` desde el primer intento (la regla ya aprendida de los 8 `down()` rotos del Paso L). **Bug de tooling encontrado al generar la primera migración, no de producción:** un comentario SQL con backticks tipo Markdown (`` `publica` ``) dentro del template literal de TypeScript de la migración cerraba el string antes de tiempo — `esbuild` fallaba con "Expected ')' but found 'publica'". Corregido quitando los backticks del comentario.

- [x] **Cambio de contraseña + cierre de sesiones agregados a "Seguridad de mi cuenta" (2026-08-04).** A pedido del usuario, tras el incidente de 2FA (ver bullet siguiente): "agrega el cambio de contraseña y qué más se le puede colocar".
  **`src/actions/cambiar-password.ts`**: exige reingresar la contraseña actual (`payload.login()`, mismo criterio que ya usa `desactivarDosFA` — una sesión secuestrada no debería poder tomar la cuenta solo cambiando la contraseña), valida mínimo 8 caracteres, y actualiza con `overrideAccess`. No toca `sessions[]` — cambiar la contraseña no cierra otras sesiones a propósito, es una acción separada.
  **`src/actions/cerrar-todas-sesiones.ts`** + `PanelSesiones.tsx`: vacía `sessions[]` por completo, **incluida la sesión propia** — decisión deliberada: preservar "esta sesión" exigiría decodificar el `sid` del JWT actual, y el caso de uso real ("no sé si alguien más tiene acceso a mi cuenta") ya se resuelve mejor cerrando todo y volviendo a entrar. También muestra `ultimo_acceso` (campo que ya existía, nunca se mostraba en ningún lado) y la cantidad de sesiones activas.
  **Verificado vía script contra la API Local** (mismos pasos que ejecutan las Server Actions): contraseña actual incorrecta rechazada; contraseña vieja deja de servir tras el cambio y la nueva funciona; `sessions[]` pasa de 3 a 0 tras cerrar todas. **No se verificó por navegador esta vez** — ver el hallazgo de abajo sobre por qué, con datos reales de por medio, convenía no arriesgarlo. `tsc --noEmit` limpio.
- [x] **Incidente real: 2FA quedó activado sin querer en la cuenta del fundador — causa raíz encontrada.** El usuario reportó que al intentar entrar con su cuenta real le pedía un código TOTP que nunca configuró. Se confirmó en la base que la cuenta (`contefania3@gmail.com`) sí tenía `dosFA_habilitado: true` con un secreto real cargado — no era un error de pantalla. Se desactivó el 2FA de esa cuenta de inmediato (`overrideAccess`, sin necesitar la contraseña) para restaurar el acceso.
  **Causa raíz, encontrada al verificar la página nueva de "Seguridad de mi cuenta" con un usuario de prueba:** la pestaña del navegador de pruebas (`Browser` del agente) **comparte las cookies de sesión con la sesión real del usuario en la misma pestaña** — no son navegadores aislados. Confirmado navegando a `/api/users/me` en medio de una prueba con una cuenta descartable recién creada y encontrando la sesión real del usuario (`contefania3@gmail.com`, con dos sesiones activas creadas en los minutos exactos de las pruebas de 2FA de la sesión anterior). Abrir una pestaña nueva **tampoco aísla nada** — las cookies son por origen, no por pestaña, así que cualquier prueba de login/2FA en ese navegador puede terminar operando sobre la cuenta real si el usuario lo está usando al mismo tiempo.
  **Cambio de método a partir de acá:** las pruebas que requieren una sesión autenticada y pueden mutar estado sensible (login, 2FA, contraseña, cierre de sesiones) se verifican contra la API Local de Payload o con `curl` + un cookie-jar aislado (`curl -c archivo.txt`, nunca el navegador compartido) — no contra el navegador de pruebas, que ya demostró interferir con sesiones reales. El navegador de pruebas sigue siendo válido para features sin riesgo de colisión de sesión (páginas públicas, formularios sin datos sensibles de otra cuenta). El usuario preguntó cómo se activa el 2FA (Paso H, sesión anterior) y la respuesta honesta fue: no hay ninguna pantalla — los tres endpoints (`/2fa/generar`, `/2fa/confirmar`, `/2fa/desactivar`) estaban probados por HTTP directo, pero nadie sin acceso técnico podía usarlos. Nueva página `/[locale]/cuenta/seguridad` (cualquier rol autenticado, un solo componente `FormularioDosFA.tsx` — la lógica de los endpoints no distingue rol) enlazada desde el header de `/portal` (becarios) y `/staff` (staff/directiva/admin), junto al botón de cerrar sesión.
  **Flujo:** botón "Activar" → `POST /2fa/generar` muestra el QR (imagen) y la clave en texto para carga manual → confirmar con el primer código real activa `dosFA_habilitado`. Con 2FA activo, "Desactivar" exige reingresar la contraseña (mismo endpoint ya construido, ahora con UI real). Ningún endpoint nuevo — la página es pura UI sobre lo que ya existía.
  **Investigación de una anomalía durante la verificación, no reproducida como bug real:** un primer test end-to-end (activar → cerrar sesión → iniciar sesión) pasó directo al panel sin pedir el código, y la base mostraba `dosFA_habilitado: false` después — parecía que el login revertía la activación. Se aisló el problema con pruebas controladas: confirmar y chequear la base inmediatamente (sin login de por medio) → se mantiene `true`; un login limpio (una sola petición) → pide el código correctamente y no toca el campo; dos logins concurrentes a propósito (`Promise.all`) → tampoco lo corrompen. Con eso descartado, se completaron los tres flujos reales de punta a punta por la UI (activar con el código TOTP real calculado a partir de la clave mostrada en pantalla — mismo algoritmo RFC 6238 que ya usa `src/lib/totp.ts`, replicado en un script aparte solo para la prueba; login en dos pasos completo con sesión emitida; desactivar rechazando contraseña incorrecta y aceptando la correcta). La anomalía original coincidió con un burst de 3 POST a `/login` — consistente con el mismo tipo de flakeo del navegador de pruebas que ya se documentó varias veces en esta sesión (clics duplicados/renderizados fantasma), no con el código de producción. Datos y usuario de prueba borrados.
  **Aparte, a pedido del usuario:** el botón "Portal" se sacó del header público y se movió al footer — no es un cambio de seguridad (`/portal` sigue siendo accesible por URL directa, la protección real es la autenticación), es de diseño: el header es ruido con un CTA de login para el 95% de quien visita el sitio y no es staff ni becario.

- [x] **Cobertura `/staff` vs. `/admin` — 5 gaps cerrados (2026-08-03).** El usuario pidió una auditoría de qué colecciones seguían exigiendo `/admin` para operarse. Resultado: `Necesidades` (pipeline completo, sin ni un botón), `Sedes`, `CentrosEducativos`, `Programas` (cero UI de staff) y `Configuracion` (global, solo lectura indirecta vía `TabLaborSocial`). Se cerraron los 5, en orden de impacto operativo, cada uno con su propio verificado antes de pasar al siguiente:
  1. **Pipeline de Necesidades** (`src/actions/actualizar-necesidad.ts`, `src/components/staff/AccionesNecesidad.tsx`, editado `/[locale]/directiva/necesidades/page.tsx`): antes la cola era 100% de solo lectura para todos los roles — ni directiva ni staff podían cambiar `estado`/`prioridad`/`visible_publicamente` ni vincular un `proyecto_resultante` sin abrir `/admin`. La acción nueva replica a propósito el mismo criterio que ya tiene `Necesidades.access.update` (`esStaffOSuperior`): solo staff/admin gestionan, directiva sigue viendo la cola pero sin controles — no fue necesario tocar el `access` de la colección, la UI solo se condiciona en `puedeGestionar`.
  2. **Sedes** y **3. Centros Educativos** (`crear-sede.ts`/`editar-sede.ts`/`FormularioSedeModal.tsx`, `crear-centro-educativo.ts`/`editar-centro-educativo.ts`/`FormularioCentroEducativoModal.tsx`, dos secciones nuevas al final de `TabComunidades.tsx`): comparten pestaña con Comunidades porque las tres son colecciones de ubicación con el mismo shape (`comunidad` + `coordenadas`). **Alcance recortado a propósito:** el campo `Sedes.fotos` (upload `hasMany`) no se expuso en el formulario — no vale la pena la complejidad de un uploader múltiple para un campo opcional que casi no se usa; el patrón de galería multi-archivo si se necesitara ya existe, ver `FormularioActividadModal.tsx` más abajo.
  4. **Programas** (`crear-programa.ts`/`editar-programa.ts`/`FormularioProgramaModal.tsx`, sección nueva en `TabProyectos.tsx`, reutiliza el array `programas` que esa pestaña ya cargaba para el dropdown de Proyectos — sin fetch nuevo en `staff/page.tsx`).
  5. **Configuracion** (`editar-configuracion-global.ts`/`FormularioConfiguracionModal.tsx`, sección nueva en la pestaña Becarios de `staff/page.tsx`): meta de horas, calificaciones reprobatorias (array simple, mismo patrón add/remove que las opciones de Prácticas), aviso de suspensión, contacto institucional, fecha de cifras de impacto. No hace falta gating de rol adicional en la UI — `/staff` entero ya es staff/admin-only a nivel de página.
  **Bug propio encontrado y corregido durante la verificación, no en el código de producción:** el script de QA que restauraba `Configuracion.texto_aviso_suspension` a su valor original pasaba `undefined` para "no cambiar nada" — pero en una actualización parcial de Payload, `undefined` significa "campo omitido", no "vaciar campo", así que el valor de prueba (`"QA aviso de prueba"`) quedó pisando el real en ambos locales hasta que se detectó en la verificación visual y se corrigió con `null` explícito. La acción de producción (`editar-configuracion-global.ts`) usa el mismo patrón `?.trim() || undefined` que ya usan `crear-comunidad.ts`/`editar-comunidad.ts` para campos opcionales — consistente con el resto del código base, no es una regresión nueva, pero **si algún día un campo de texto opcional necesita poder vaciarse explícitamente desde un formulario de edición, ese patrón no alcanza** y hay que pasar `null` a propósito.
  **Verificación de cada gap, no solo compilado:** los 5 se probaron end-to-end contra Postgres real vía scripts desechables con la API Local de Payload (creado, editado, y confirmado que un becario de prueba recibe 403/rechazo en cada colección — mismo camino de acceso que usa la Server Action, `overrideAccess:false` + `user` real) y visualmente en el navegador logueado como staff de prueba (cuenta descartable, nunca la contraseña real del usuario) — capturas de cada sección nueva, un ciclo completo de crear/editar en Sedes, y confirmación de que directiva sigue sin ver los controles de Necesidades. Usuario y datos de prueba borrados al terminar. `tsc --noEmit` limpio en cada paso (el proyecto tiene ~1500 errores preexistentes en `node_modules/@maplibre/maplibre-gl-style-spec` — corrupción del `.d.ts` de esa dependencia, cero relación con este cambio, cero errores bajo `src/`).
  **Gaps identificados pero dejados fuera a propósito** (no son bugs, son decisiones ya tomadas en fases previas): Niveles/Materias siguen linkeando a `/admin` (taxonomías de un solo campo); creación de cuentas staff/directiva/admin sigue exigiendo `/admin` (decisión deliberada de Paso I: solo becarios se invitan desde el panel); no hay visor de `Auditoria` en `/staff` todavía.

- [x] **Publicaciones (Actividades) traídas al panel — sin editor WYSIWYG completo (2026-08-03).** El usuario probó el link-out a `/admin` y lo encontró poco intuitivo ("creo que lo complica más"), así que se cerró el único gap que la auditoría anterior había dejado fuera a propósito por su complejidad (rich text + galería multi-archivo).
  **Simplificación deliberada del campo `contenido` (Lexical richText):** en vez de embeber un editor WYSIWYG completo, el formulario usa un `<textarea>` de texto plano donde una línea en blanco separa párrafos — sin negrita/enlaces/listas. Nuevo helper compartido [src/lib/richtext.ts](file:///home/fabianc/Documentos/ForumPage/src/lib/richtext.ts): `textoAParrafos` arma el JSON mínimo de Lexical (un nodo `paragraph` por bloque separado por `\n\n`) y `parrafosATexto` hace el camino inverso para precargar el textarea al editar, uniendo los párrafos existentes con `\n\n` — a diferencia del `extractTextFromRichText` que ya usaba `staff/page.tsx` para Nosotros (que junta todo con espacios y pierde los saltos de párrafo), acá sí importa preservarlos porque el contenido es de longitud de artículo, no una frase.
  **Portada y galería con el mismo patrón de subida que ya usa `crear-equipo.ts`** (`FormData` → `payload.create({collection:'media', file:...})` → id), extendido a múltiples archivos para la galería (`formData.getAll('files')`, loop). Al editar, cada foto de la galería existente tiene su botón "✕" que la saca del array antes de guardar — las nuevas se agregan con un input `multiple` aparte; no hay reordenamiento ni edición de metadatos por foto, deliberadamente fuera de alcance.
  **Verificado:** helper de richtext con test de ida y vuelta (`textoAParrafos` → `parrafosATexto` reproduce el texto original); creación con portada + 2 fotos de galería y edición que saca una foto, contra Postgres real vía script con la API Local (mismo `overrideAccess:false` + `user` que la Server Action); becario rechazado (403) al intentar crear una actividad. En el navegador: edición de una publicación real del seed mostró el título/extracto/contenido/comunidad/portada/galería precargados correctamente (el contenido con sus párrafos intactos), remover una foto de la galería funcionó (cancelado sin guardar para no tocar datos reales); creación de una publicación nueva de punta a punta con subida de imagen real vía `DataTransfer` (mismo mecanismo que un navegador real), confirmando que aparece en `/es/historias` con slug autogenerado correcto y los dos párrafos renderizados por separado. Datos de prueba borrados después. `tsc --noEmit` limpio bajo `src/`.
  **Gap real encontrado después, a partir de una pregunta del usuario** ("¿revisaste que las portadas se recorten bien, con previsualización?") — no lo había chequeado. El sitio ya usa un mecanismo de "punto focal" (`Media.focalX`/`focalY`, que Payload agrega automáticamente a toda colección con `upload`) para decidir qué parte de la portada no se recorta, tanto en el hero de `/historias/[slug]` como en la galería — con default 50/50 (centro) si no se fija. El formulario nuevo subía la imagen sin fijar nunca ese punto (quedaba siempre en el centro por defecto), y **sin ninguna previsualización de cómo se vería recortada** — antes, editando por `/admin`, el subidor nativo de Payload trae su propio editor de imagen con esa herramienta integrada; al traer la subida al panel simplificado, se perdió esa capacidad sin avisar.
  **Segundo hallazgo, más grave, al revisar el código de golpe:** [`ActividadCard.tsx`](file:///home/fabianc/Documentos/ForumPage/src/components/ActividadCard.tsx) — la tarjeta que se ve en el listado de `/historias` (fuera de la publicación individual) — **nunca leía `focalX`/`focalY`, ni antes de esta sesión**. Siempre recortaba al centro sin importar qué punto focal tuviera la foto seteado desde `/admin`. Bug preexistente, no introducido en este cambio, pero que la pregunta del usuario hizo aflorar.
  **Corregido:** `ActividadCard.tsx` ahora aplica `objectPosition` con `focalX`/`focalY` igual que el hero y la galería (una línea, mismo patrón ya usado en los otros dos lugares). `FormularioActividadModal.tsx` agrega una previsualización de portada recortada a `aspect-4/3` (el recorte más ajustado, el de la tarjeta) — clic sobre la imagen fija el punto focal, con un marcador visual y botón "Restablecer centro"; se resetea a centro cuando se elige un archivo nuevo. `crear-actividad.ts`/`editar-actividad.ts` guardan `focalX`/`focalY` al crear el documento de media, y si la portada no cambia pero el staff sí movió el punto focal, se actualiza el documento de media existente con un `payload.update` aparte (la foto no se vuelve a subir).
  **Verificado con una imagen mitad roja/mitad azul** (generada con `<canvas>` en el propio navegador vía `javascript_tool`, sin depender de un archivo externo) para poder distinguir visualmente el recorte: clic en la franja roja fijó foco en `(36%, 15%)`; tras publicar, se leyó el `style` computado de la imagen tanto en la tarjeta de `/historias` como en el hero de `/historias/[slug]` — ambas mostraron `object-position: 36% 15%`, coincidiendo exactamente. De paso, esto confirmó que el arreglo de `ActividadCard.tsx` también corrige la vista de tarjeta para el contenido real ya existente que sí tenía puntos focales seteados desde antes (ej. `46% 62%` en un post real del seed) y que hasta ahora se ignoraban ahí. Datos de prueba borrados. `tsc --noEmit` limpio.

- [x] **Centro de Aprendizaje en `/staff` + pulido de UI pública (2026-08-03).** El staff no tenía forma de publicar Recursos/Tutorías/Prácticas sin pasar por `/admin` — justo lo que `/staff` existe para evitar. Nueva pestaña con CRUD completo vía modales para las tres colecciones (Prácticas incluye armado de preguntas/opciones en el mismo panel, sin salir a `/admin`, con validación de que cada pregunta tenga al menos 2 opciones completas y un índice de respuesta correcta válido); `Recursos.archivo`/`Practicas.archivo` suben a `media` a propósito — es material público de la Biblioteca, no expediente privado. Con 6 pestañas la barra horizontal dejó de entrar en la ventana y forzaba un scrollbar — pasa a panel lateral vertical en desktop (md+), sigue horizontal con scroll en mobile (no hay espacio para un sidebar ahí).
  **Bug real encontrado probando con 20 preguntas reales (a pedido del usuario, no hipotético):** el modal usaba `items-center` para centrarse verticalmente; con contenido más alto que la ventana eso atasca el scroll del navegador — el título quedaba en `y: -306px`, fuera del viewport y sin forma de scrollear hasta ahí (bug conocido de Chromium con flexbox centrado + overflow). Corregido a `items-start` en los tres modales nuevos (Recurso, Tutoría, Práctica).
  **Las 3 modalidades de Práctica verificadas de punta a punta, no solo compiladas:** autocorregido con 20 preguntas reales persistidas correctamente en Postgres (`respuesta_correcta`/`retroalimentacion` confirmadas ausentes de `/api/practicas` sin sesión); con progreso, calificando y recargando la página para confirmar "Completado antes: X/Y" desde `localStorage`; descargable subiendo un archivo real vía `DataTransfer` (mismo mecanismo que usaría un navegador real) y confirmando la descarga con `curl` contra `/api/media/file/...`. Datos de prueba borrados después en los tres casos.
  **Además:** filtros por modalidad/nivel/materia + paginación en `/aprende/practicas` (reutiliza `FiltrosBiblioteca` tal cual, sin duplicar el componente); "Ver" y "Descargar" separados para PDF propio en Biblioteca (antes un solo botón forzaba descarga siempre); selector de idioma con banderas 🇵🇦/🇺🇸 en vez de texto plano "EN"/"ES" (se veía pegado al logo en mobile, sin contenedor); se sacó el botón "Portal de equipo" que Antigravity había agregado a la página pública de `/impacto` (enlazaba a `/portal`, autenticado — un visitante anónimo solo chocaba con un login). Se agregaron los niveles/materias que faltaban (Universidad; Química, Física, Biología, Historia, Geografía) vía script de datos contra Payload, nunca hardcodeados en código.
  `tsc`/`lint` limpios en cada uno de los 5 commits. Sin migraciones — ningún cambio de esquema en todo este bloque.
- [x] **Auditoría del trabajo de Antigravity (21 commits directos a `main`, 2026-08-01/02) + fuga de consentimiento de becarios encontrada y corregida.** El usuario pasó a trabajar con Antigravity como programador principal por límite de uso acá; esta entrada es la primera revisión de lo que produjo sin supervisión de Claude. Alcance: CRUD completo de staff para Comunidades/Proyectos/Equipo/Nosotros (9 server actions nuevas + modales), 16 corregimientos de Penonomé cargados en `Comunidades`, mapa base real (CartoDB/OSM) con etiquetas de poblado, mini-mapa en el Home, ficha de comunidad con sus becarios originarios, y tarjeta de "Becario Destacado" en `/impacto`. `tsc`/`eslint` limpios en cada commit, CI en verde salvo un fallo puntual (912048b) corregido por el commit siguiente. Ninguna migración de esquema nueva (se verificó con `git diff --stat` contra `src/collections`/`src/migrations`: sin cambios).
  **Encontrado: `overrideAccess: true` sin repetir el filtro de consentimiento.** Las 9 acciones nuevas sí respetan el access control (`overrideAccess: false` + `user`, patrón correcto). Pero dos lecturas públicas de `becarios` usan `overrideAccess: true` (necesario para leer más allá de lo que un becario individual podría, ya que la página no tiene sesión) y **no reponen a mano el filtro que `lecturaBecarios` aplicaría para un visitante anónimo** (`{ mostrar_en_mapa: { equals: true } }`):
  1. `src/app/(frontend)/[locale]/impacto/comunidades/[slug]/page.tsx` — listaba **todos** los becarios de la comunidad (nombre, universidad, carrera, país) sin mirar `mostrar_en_mapa`.
  2. `src/app/(frontend)/[locale]/impacto/page.tsx` — la tarjeta "Becario Destacado" (`becarioDestacadoRaw`) tampoco lo miraba, pese a que **la misma función, dos líneas arriba**, sí lo aplica correctamente para los pines del mapa (`becariosFeatures`, `if (!b.mostrar_en_mapa || ...) return []`) — inconsistencia dentro del propio archivo, no una omisión general.
  Con los 6 becarios de dev (todos `mostrar_en_mapa: true`) no se manifestaba, pero el código estaba roto: el primer becario real que apague ese campo — el mecanismo explícito que el propio formulario le ofrece para revocar consentimiento en cualquier momento — igual aparecería con nombre, foto y cita en una página pública. **Corregido** replicando el mismo filtro que ya usa `becariosFeatures`, en los dos puntos. `tsc`/`eslint` limpios. No se pudo verificar en el navegador de este sandbox (el dev server lanzado por la herramienta de preview no levanta `PAYLOAD_SECRET` desde `.env` — falla igual en páginas sin tocar, como el Home; problema de entorno, no del cambio); queda para confirmar con `pnpm dev` normal.
  **Encontrado, no corregido — hardcodeo real:** el selector de "Destinos Internacionales Frecuentes" (Bocconi, University of Florida, Navarra, Tec de Monterrey, EARTH, Zamorano) vive como un `if/else` con nombre/país/ciudad/coordenadas fijos, duplicado en `FormularioNuevoBecario.tsx` y `FormularioEditarBecario.tsx`. Viola el no-negociable "nada hardcodeado" de CLAUDE.md en espíritu (no es una taxonomía en el sentido literal de comunidades/programas/niveles/materias, pero es exactamente el mismo patrón: datos que un staff sin acceso a código no puede editar ni ampliar). No es una falla de seguridad, así que no se tocó en esta pasada — señalado para cuando se aborde.
  **Encontrado: `docs/plan.md` no se tocó en ninguno de los 21 commits.** Viola "Docs y specs actualizados en el mismo cambio que el código" de CLAUDE.md. La bóveda de Obsidian sí se actualizó parcialmente (2 de 17 notas — `🗺️ Mapa de Impacto & MapLibre.md` y `🚀 Plan de Ejecución & Estado de Fases.md`), pero ese archivo es un espejo de consulta, no la fuente que gobierna — quedó por delante de este documento en vez de al revés. Las dos notas tampoco mencionan el filtro `mostrar_en_mapa` al describir las mismas dos features del hallazgo de arriba, reflejando el mismo hueco que tenía el código.
  **Reglas nuevas agregadas a `.agents/AGENTS.md`** (el archivo de directivas que usa Antigravity) para que esto no se repita: reponer el filtro de consentimiento cuando se usa `overrideAccess: true` en una página pública, no hardcodear listas que el staff debería poder editar, y actualizar `docs/plan.md` en el mismo commit que cierra una feature — ver esa sección para el detalle completo.

- [x] **Remediación de privacidad de documentos — migración confirmada en dev y ya aplicada en el VPS real (nota corregida 2026-08-04, ver Fase 1 Paso Q).** Verificado contra el Postgres de dev: las 17 migraciones acumuladas están aplicadas (`payload_migrations`, todas batch 1), y `pnpm purgar:media-privada` da 0 filas a borrar hoy (el único caso, `media 869`, sigue siendo el duplicado de seed ya documentado abajo, no una fuga real). Correr `payload migrate` contra producción — que en su momento estaba bloqueado por no existir todavía ningún servidor real — se hizo el 2026-08-04 contra el VPS del Paso Q, junto con dos migraciones más acumuladas desde entonces (19 en total). Los campos `Becarios.documentacion_socioeconomica`, `RegistrosAcademicos.documento`, `HorasLaborSocial.evidencia` y `Recuperaciones.evidencia` vivían en `Media`, que es `read: () => true` — cualquiera con la URL descargaba la documentación socioeconómica de un becario o evidencia de labor social que puede contener menores. Viola el no-negociable de "buckets separados" de CLAUDE.md. Se creó `DocumentosPrivados` (solo-staff en las cuatro operaciones) y los cuatro campos ahora apuntan ahí.
  **`Recuperaciones.evidencia` se pasó por alto en la primera pasada** y se corrigió después (migración `20260801_193645_evidencia_recuperaciones_privada`, con la misma copia de datos id-preservando + `REPLACE` de url). Es el campo más delicado de los cuatro: una recuperación solo existe si el becario estuvo suspendido, y CLAUDE.md marca como no-negociable que ese estado no se hace público **ni de forma agregada** — la sola existencia del archivo bajo `/api/media/` lo delataba.
  **Decisión de acceso:** solo-staff, no "propio o staff". `BotonVerDocumento` se usa únicamente en `src/components/staff/`; ninguna vista del becario renderiza estos archivos, así que darle lectura sería ampliar el alcance sin que nadie lo pida. El campo `uploadedBy` quedó como auditoría pura — **no** se usa para control de acceso, porque registra quién apretó el botón y el staff sube documentación en nombre del becario todo el tiempo.
  **Migración en dos mitades, las dos obligatorias:** la migración SQL (`20260801_190433`) copia las filas de `media` a `documentos_privados` **preservando el id**, que es lo que mantiene válidas las FK sin remapear nada, y reescribe la `url` a la ruta nueva. Pero copiar no es mover: la fila original sigue en `media` y por lo tanto sigue siendo pública. `pnpm purgar:media-privada` cierra eso — mueve el archivo físico y borra el original con `payload.delete` (que se lleva también las derivadas thumbnail/card/hero/og, igual de públicas). Empareja copia con original por `filename`, no por id: el id de un documento privado creado después de la migración puede coincidir por casualidad con el de una foto pública, porque las dos secuencias avanzan por separado. Antes de borrar consulta el catálogo de Postgres para ver qué columnas siguen apuntando a `media(id)` — en vez de hardcodear la lista, que hoy son 13 y mañana más — y omite cualquier fila todavía referenciada. Sin `--purge` hace un simulacro.
  **Bug que rompía la app, encontrado al revisar:** `reportar-horas.ts` seguía subiendo la evidencia a `collection: 'media'` mientras `HorasLaborSocial.evidencia` ya apuntaba a `documentos-privados` — el id de media contra una FK a `documentos_privados` reventaba el reporte de horas con evidencia, y una vez que las secuencias se cruzaran habría enlazado el documento de otro becario en silencio.
  **Bug del script de purga, encontrado al correrlo:** el filtro del catálogo excluía `payload_%` pero no `media_locales`, que es el sidecar de localización de la propia `media`. Como `alt` es required y localized, **las 867 filas de media tienen fila ahí**, así que el script se declaraba a sí mismo que todo seguía referenciado y borraba 0 siempre — era un no-op por construcción. Se excluye ahora esa tabla puntual y no `%_locales` en general: el sidecar de otra colección sí puede tener un upload localizado apuntando a media, y esa es una referencia real que debe frenar el borrado.
  **Verificación (2026-08-01, contra el Postgres de Docker `forumpage-db-1`):** `tsc` limpio; `eslint` sin errores en el repo (los 9 que aparecen salen de eslint escaneando `.claude/worktrees/`, copias viejas — conviene ignorar esa ruta). GET sin sesión contra `/api/`: `/api/recuperaciones` 403 ✓, `/api/documentos-privados` 403 ✓, `/api/documentos-privados/869` 403 ✓, `/api/documentos-privados/file/<nombre>` 403 ✓.
  **Los documentos privados estaban commiteados en git.** `.gitignore` tenía `media/` pero nunca `documentos-privados/`, así que `a0919e3` — el propio commit "feat(security)" — versionó los 44 archivos del directorio de subidas, y ya está en `origin/main`. Se agregó la regla al `.gitignore` y se sacaron del índice con `git rm --cached` (siguen en disco, que es de donde Payload los sirve). El repo es privado en GitHub, así que no hubo exposición pública, y los archivos siguen en el historial del remoto. **Decisión: no se reescribe el historial.** Son fotos que ya eran públicas en el WordPress viejo, no documentación confidencial; reescribir historia ya publicada (`git filter-repo` + force push, que invalida todo clon existente) es costo sin beneficio. **Condición para revisarlo:** si el repo alguna vez pasa a público, suma colaboradores externos, o si en `documentos-privados/` llega a haber documentación socioeconómica real commiteada, ahí sí hay que reescribir.
  **Migraciones probadas contra Postgres limpio (2026-08-01).** Se levantó un contenedor aparte y se corrió la cadena entera: **17 arriba y 17 abajo, sin errores, cero tablas restantes**. La copia de datos de la migración nueva se probó dirigida (no basta con base vacía, donde es no-op): se insertó una fila en `media` usada como `evidencia` de una recuperación, se aplicó la migración y quedó copiada a `documentos_privados` **con el id preservado** (500 → la FK sigue válida sin remapear), la url reescrita a `/api/documentos-privados/file/…` y la fila de `alt` copiada al sidecar de locales.
  **`down()` estaba roto en 8 migraciones.** El patrón que genera Payload hace `DROP TABLE … CASCADE` y después un `DROP CONSTRAINT` explícito de constraints que el CASCADE ya se llevó — revienta con `constraint … does not exist` y deja el rollback a medias. Antes del arreglo la cadena hacia abajo moría en la migración 15 de 17. Se agregó `IF EXISTS` a los `DROP CONSTRAINT`/`DROP INDEX` **solo dentro de `down()`**: en `up()` un DROP que falta sí debe fallar ruidosamente. Si en el futuro una migración generada crea una colección, revisar que su `down()` traiga `IF EXISTS`.
  **Base de dev alineada con las migraciones.** Estaba en modo push (`payload_migrations` con una sola fila `name='dev'`): esquema al día pero ninguna migración corrida nunca, que es justamente por qué el `down()` roto pasó meses sin detectarse. Se verificó primero que el esquema empujado fuera equivalente al que producen las migraciones — **444 columnas y 96 FK, diferencia cero** contra una base construida solo con la cadena — y recién entonces se marcaron las 17 como aplicadas (batch 1), sin recrear nada ni perder datos de seed. Además `push: false` en el adaptador: sin eso el dev server empuja el próximo cambio de esquema solo y la base vuelve a divergir. De acá en adelante, cambiar el esquema en dev exige `migrate:create` + `migrate`, igual que producción.
  **Auditoría de los `upload` restantes (los 13 del pedido original, menos los 4 ya movidos):** quedan 11 apuntando a `media` y los 11 son públicos a propósito — `Proyectos.foto_antes`/`foto_despues`, `Sedes.fotos`, `Actividades.portada`/`galeria`, `Comunidades.foto`, `Equipo.foto`, `Nosotros.foto`/`logo`, `Recursos.archivo` y `Practicas.archivo` (material descargable de la Biblioteca, la colección entera es `read: () => true` por diseño). Ninguno guarda datos de expediente. No hay un quinto campo mal ubicado.
  **El único que exigió mirar dos veces fue `Becarios.foto`**, y tenía un hueco real, chico pero concreto: el *documento* del becario sí estaba protegido (para anónimos `lecturaBecarios` devuelve `{ mostrar_en_mapa: { equals: true } }`, y `mostrar_en_mapa` no valida sin `consentimiento_firmado`), pero el *archivo* vivía en `media`, que es `read: () => true`. O sea que un becario que apagaba `mostrar_en_mapa` desaparecía de `/api/becarios` pero su foto seguía sirviéndose en `/api/media/file/<nombre>` para siempre — justo lo contrario de lo que la descripción del campo promete ("puede revocar su consentimiento apagando esto en cualquier momento"). **Cerrado el 2026-08-04, ver la entrada "Becarios.foto: privacidad condicionada al consentimiento" más abajo** — no hizo falta esperar a los buckets separados de la línea de abajo: el mismo control de acceso declarativo de Payload que ya protege `DocumentosPrivados` (una condición en `access.read`, evaluada en cada request, no un bucket con URL firmada) alcanza para un caso condicional como este.
  **Lo que sigue abierto:** `/api/media/file/<nombre>` **sigue devolviendo 200**: el original público no se borró porque `media 869` es a la vez portada de una actividad pública y el documento privado de `registros_academicos 16` / `horas_labor_social 13` — cruce de datos del seed, no defecto del código. Con datos reales el script ya purga bien. `recuperaciones` está vacía (0 filas), así que la copia de datos de la migración nueva es no-op hoy y no se pudo probar con datos. En disco, `documentos-privados/` tiene 44 archivos y la base referencia **uno solo** (el 869): los 44 son byte a byte idénticos a su gemelo en `media/`, así que son copias muertas de fotos que ya eran públicas — basura del seed, no fuga. Se dejan (están gitignorados); si molestan, se borran a mano salvo `Yilka-Eliseth-Espinoza-Medina-4.jpeg` y sus tres derivadas.

- [x] **Paso I — Alta de becarios por invitación.** `01-documento-de-proyecto.md` §Operación: "creada por el staff mediante invitación por enlace. No hay autorregistro". Reutiliza el mecanismo de "olvidé mi contraseña" que Payload ya trae (`resetPasswordToken`/`resetPasswordExpiration`, vence en 1 hora, un solo uso — cumple 05-ciberseguridad.md §3.6 sin código nuevo) en vez de inventar un sistema de tokens propio.
  **`Users.access.create`** pasó de `esAdmin` a `esStaffOSuperior` (el staff ya puede invitar, antes solo un admin podía). Para que ampliar esto no abriera una escalada de privilegios, se agregó un `validate` al campo `rol` que rechaza `rol: 'admin'` cuando el actor autenticado no es admin — **bug propio encontrado en el camino:** la primera versión del `validate` también rechazaba las creaciones internas (`overrideAccess: true`, sin `req.user`, como los scripts de prueba o un futuro seed del primer admin), porque `validate` corre siempre, `overrideAccess` no lo salta. Corregido para solo bloquear cuando hay un actor autenticado real intentando escalar.
  **Flujo:** el staff crea la cuenta desde el alta estándar de becarios en el panel (con cualquier contraseña de relleno — Payload la exige, pero no importa cuál sea). Un hook `afterChange` (`generarInvitacionAlCrear`, solo dispara para `rol: 'becario'` — **no** para staff/directiva/admin, que se siguen dando de alta con una contraseña real puesta directamente, igual que el primer usuario admin) 1) sobrescribe esa contraseña de relleno con una aleatoria de 32 bytes que nadie conoce, 2) genera el token de reseteo vía `payload.forgotPassword({ disableEmail: true })`, y 3) guarda el enlace completo (`/es/cuenta/activar?token=...`) en un campo nuevo `enlace_invitacion` (staff/admin-only, nunca vía API para nadie más) para que el staff lo copie y lo mande — nadie entra a la cuenta antes que la persona invitada.
  **Frontend nuevo** (primera página real del Portal del Becario en este repo): `/[locale]/cuenta/activar` lee `?token=` y muestra un formulario de contraseña+confirmación (`src/components/ActivarCuenta.tsx`) que llama directo a `/api/users/reset-password` (el endpoint de Payload, sin tocar — no hace falta un endpoint propio para esta parte). Al confirmar, la cuenta queda activa y logueada.
  Verificado con peticiones HTTP reales: staff (no admin) invita a un becario nuevo ✓; la contraseña de relleno que tecleó el staff no sirve para entrar ✗→✓ (401 esperado); `enlace_invitacion` se genera solo ✓; directiva no puede crear cuentas (403) ✓; staff no puede invitar con rol admin (400) ✓; activar con el token real funciona (200) ✓; login con la contraseña recién elegida por la propia persona funciona ✓; `enlace_invitacion` confirmado **ausente** de `/api/users/me` para el propio becario ✓; reusar el mismo token ya consumido falla (403) ✓. Página de activación probada en navegador real (sin token: mensaje de enlace inválido; con token: formulario → éxito → enlace a `/admin`), sin errores de consola. Build de producción + `check:budget` limpio (162.2 KB / 500 KB).
  **Gap conocido en su momento, ya cerrado (nota corregida 2026-08-04):** el `/api/users/reset-password` por defecto de Payload emitía sesión directo sin pedir 2FA — para una cuenta ya activada con 2FA habilitado, "olvidé mi contraseña" saltearía el segundo factor. Cuando se construyó "Recuperar contraseña" (`/cuenta/recuperar` + `/cuenta/restablecer`, ver la entrada de Fase 3 más abajo), el endpoint `/reset-password` se reemplazó por un handler propio (`restablecerContrasena` en `Users.ts`) con el mismo patrón de desafío en dos pasos que `/login`: si la cuenta tiene `dosFA_habilitado`, devuelve `desafioId` en vez de la cookie, y `FormularioContrasena.tsx` pide el código antes de terminar. Confirmado leyendo el handler actual, no solo asumido — esta nota había quedado desactualizada.
- [x] **Paso H — 2FA TOTP: enrolamiento, login en dos pasos, y desactivación segura.** Marcado CRÍTICO en `05-ciberseguridad.md`. Implementado con `crypto` del stdlib (`src/lib/totp.ts`, HMAC-SHA1 + truncamiento dinámico de RFC 6238/4226 — **verificado contra los 4 vectores de prueba oficiales del Apéndice B de la RFC 6238 antes de integrarlo**, sin librería externa de TOTP) y un único paquete nuevo, `qrcode`, solo para renderizar el QR de enrolamiento (no existía nada instalado que lo cubriera).
  **Campos en `Users`:** `dosFA_habilitado` (checkbox, de solo lectura vía API salvo admin) y `dosFA_secreto` (texto, `admin.hidden`, con `access: { create: () => false, read: () => false, update: () => false }` — **nunca** viaja por la API, ni al dueño ni a un admin, igual que una contraseña; solo se toca internamente con `overrideAccess` desde los endpoints propios).
  **Endpoints propios en `Users`** (`src/collections/Users.ts`): `POST /2fa/generar` (autogenera secreto + devuelve `otpauth://` URI y el QR como data-URI, queda pendiente hasta confirmar), `POST /2fa/confirmar` (valida el primer código real y recién ahí prende `dosFA_habilitado`), `POST /2fa/desactivar` (exige reingresar la contraseña — reautenticar antes de apagar 2FA, para que una sesión secuestrada por XSS no pueda desactivarlo por su cuenta).
  **`POST /login` reemplazado por completo** por un endpoint propio: Payload resuelve el primer endpoint que matchee path+método (`.find()`), y los endpoints declarados en la colección toman precedencia sobre las rutas automáticas. Si `dosFA_habilitado` es true, el primer paso valida usuario+password y devuelve un `ticket_2fa` efímero (JWT firmado con `secretKey`, vence en 5 minutos) en vez de la cookie final; el cliente presenta el segundo paso y llama a `POST /login/2fa` con el ticket + el TOTP actual.
- [x] **Paso J — Portal del Staff & Mejoras de UX (2026-07-30):**
  1. **Menú de Pestañas Principal:** Navegación en la parte superior del Portal del Staff entre **Becarios** y **Publicaciones**.
  2. **Buscador de Becarios:** Filtro dinámico e instantáneo en cliente por nombre en la lista de becarios para escalar a >100 registros.
  3. **Verificación Visual de Evidencias:** Evaluadores pueden revisar documentos (créditos, boletines, informes de labor social) en pestaña lateral mientras llenan la verificación.
  4. **Flujo de Desembolsos:** Simplificado a "Registrar Pago Realizado" (marca directamente como `pagado` y registra la fecha efectiva).
  5. **Paginación de Publicaciones:** Historial de publicaciones limitado a 10 elementos por página con botones de navegación.
  6. **Custom Branding en Payload Admin:** Inyección de CSS (`admin.scss`) y Logo corporativo en el panel de Payload para alinearlo visualmente al frontend (`montana`, `niebla`, `Archivo Expanded`, `Source Serif 4`).

  Login en dos pasos cuando `dosFA_habilitado` es true: paso 1 (contraseña) llama a `payload.login()` normal para validar credenciales pero **no emite la cookie todavía** — el token ya generado se guarda en un desafío efímero en memoria (`src/lib/dos-fa-desafios.ts`, TTL 5 min, un solo uso, máximo 5 intentos fallidos antes de invalidarse — mismo espíritu que el `maxLoginAttempts` que ya usa Payload para la contraseña, para no dejar los 6 dígitos abiertos a fuerza bruta) y se devuelve solo un `desafioId`; paso 2 (`desafioId` + código) recién ahí arma la cookie de sesión a mano (mismo nombre `${cookiePrefix}-token` que usa `extractJWT`, para que el panel admin y `/api/users/me` no noten la diferencia). Si el usuario no tiene 2FA habilitado, el paso 1 emite la cookie directo, sin cambios de comportamiento. **`ponytail`:** el mapa de desafíos es de un solo proceso — si el despliegue escala a varias instancias sin sticky sessions, subir a un store compartido (Redis).
  **Bug real encontrado y corregido en el camino:** la primera versión invalidaba el desafío completo con el primer intento fallido (typo con la app autenticadora = tener que reingresar la contraseña de nuevo). Corregido para que un código incorrecto solo cuente como intento (hasta el límite de 5), no consuma el desafío.
  Verificado con peticiones HTTP reales contra el servidor corriendo (no solo Local API): login sin 2FA emite cookie directo; `/2fa/generar` devuelve secreto+QR; confirmar con código incorrecto rechaza (400), con el código real (calculado independientemente a partir del mismo secreto) habilita 2FA; login con 2FA ya no emite cookie y devuelve `desafioId`; completar el desafío con código incorrecto no lo invalida (login nuevo, mismo desafío, código correcto sí funciona después); agotar los 5 intentos sí invalida el desafío incluso con el código correcto ya después; reusar un `desafioId` ya consumido falla; desactivar con contraseña incorrecta falla, con la correcta apaga 2FA; `dosFA_secreto` confirmado **ausente** (no `null`) de `/api/users/me` en todo momento. Migración generada y verificada contra un Postgres vacío (9 migraciones acumuladas, solo 2 columnas nuevas en `users`).
  **Decisión del fundador (2026-07-30): 2FA queda opcional para todos los roles, sin forzarlo en staff/directiva/admin** — reemplaza la obligatoriedad marcada `[CRÍTICO]` en `05-ciberseguridad.md` §3.6 y en `01-documento-de-proyecto.md` §Operación; ambos documentos y `docs/spec.md` se actualizaron para reflejarlo. Con esto, "3.1 Autenticación" da por cerrado el punto de 2FA — no hay una capa de enforcement pendiente. **Pendiente real de 3.1:** recuperación de contraseña, duración de sesión diferenciada por rol.
- [x] **Paso R — Verificación de la vista de directiva + auditoría activa ampliada. Cierra "3.5" y Fase 3 completa.**
  **Vista de directiva de solo lectura sobre todo el sistema:** era en gran parte una VERIFICACIÓN, no construcción — cada colección de Fase 3 ya se construyó dándole a directiva `read: true` en el `access` de la colección (nunca `create`/`update`/`delete`). Se auditó explícitamente, por API real, con una cuenta directiva de verdad contra las 20 colecciones registradas (`users`, `media`, `auditoria`, `becarios`, `comunidades`, `sedes`, `centros-educativos`, `programas`, `proyectos`, `necesidades`, `actividades`, `niveles`, `materias`, `recursos`, `practicas`, `tutorias`, `registros-academicos`, `recuperaciones`, `horas-labor-social`, `desembolsos`) más el global `Configuracion`: **lectura 200 en las 21, escritura (create/update/delete) bloqueada con 403 en las 21, sin excepciones.** La única particularidad esperada es `Users`, donde directiva (como cualquier rol) solo ve su propio registro, nunca la nómina completa — así lo pide la propia matriz de permisos (`Usuarios y roles: Directiva — "—"`), no es un hueco.
  **Auditoría activa ampliada** — hasta este paso, `Auditoria` solo registraba dos eventos automáticos (`suspension_automatica`, `reactivacion_automatica`). Se agregaron cinco eventos más, uno por cada tipo que pedía el checklist, reusando un helper nuevo (`src/lib/auditoria.ts`, `registrarAuditoria()`) en vez de repetir el `payload.create` a mano en cada colección (y se aprovechó para refactorizar los dos eventos existentes al mismo helper):
  - **Verificaciones** — `verificacion_registro_academico` (RegistrosAcademicos) y `verificacion_recuperacion` (Recuperaciones), en la transición a `verificado`, **exista o no** materia reprobada / esté o no suspendido el becario — antes solo se auditaba indirectamente cuando terminaba en suspensión o reactivación, no el acto de verificar en sí.
  - **Horas** — `aprobacion_horas` / `rechazo_horas` (HorasLaborSocial), en la transición a `aprobada`/`rechazada`.
  - **Desembolsos** — `cambio_estado_desembolso`, en cualquier transición de `estado` (manual, cuando el staff marca `pagado`/`cancelado`, o en cascada).
  - **Roles** — `cambio_de_rol` (Users), en cualquier cambio de `rol` — nunca en la creación (el rol inicial no es un "cambio").
  **Verificado explícitamente el caso más incierto antes de darlo por sentado:** ¿el `update` masivo por `where` que usa el automatismo de suspensión (retiene/libera varios desembolsos de un becario a la vez, sin iterar por id) dispara el `afterChange` **por cada documento afectado**, o solo una vez a nivel de operación? Se probó de punta a punta: verificar un registro con una materia reprobada real, y confirmar que el desembolso `programado` de ese becario específico terminó con su propia entrada `cambio_estado_desembolso` en `Auditoria` — **sí dispara por documento**, así que la cascada automática queda tan auditada como un cambio manual.
  Verificado con 8 casos por API/Local real: verificar un registro sin reprobadas audita igual ✓; verificar una recuperación con el becario no suspendido audita igual ✓; aprobar y rechazar horas auditan con acciones distintas ✓; cambiar el estado de un desembolso a mano audita ✓; la cascada automática también audita por documento ✓; crear un usuario nuevo NO genera una entrada de "cambio de rol" (sería ruido, no es un cambio real) ✓; cambiar el rol de un usuario existente sí audita ✓. `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB) — sin migración, no hay cambio de esquema.
  **Con esto, Fase 3 — Portal del Becario queda completa**: 3.1 Autenticación, 3.2 Expediente académico, 3.3 Labor social y pagos, 3.4 Experiencia del becario suspendido, y 3.5 Necesidades y cierre.
- [x] **Paso Q — Cola priorizada para la directiva.** Página interna nueva (`/[locale]/directiva/necesidades`, gateada a staff/directiva/admin — becario o sin sesión ven un mensaje corto con enlace a `/admin`), distinta de la cola PÚBLICA del Paso P: acá se ve todo (pública o no), agrupado por prioridad, con `solicitante` visible (directiva ya lo veía desde el Paso O) y costo estimado.
  **Bug de ordenamiento real, encontrado antes de escribir una sola línea de UI:** `prioridad` es un `select` de texto (alta/media/baja) — ordenarlo alfabéticamente da "alta, baja, media", no la prioridad real (baja quedaría en el medio). Se agregó un campo numérico oculto `prioridad_orden` (1/2/3), sincronizado por un hook `beforeChange` cada vez que cambia `prioridad`, y la consulta ordena por ese campo — no por el texto. Verificado explícitamente: se crearon los tres casos a propósito en un orden distinto al de prioridad (media, alta, baja) para confirmar que el ordenamiento en pantalla es por prioridad real y no por orden de creación.
  **Estructura de la página:** tres grupos (alta/media/baja) con los casos activos (todo excepto `completada`), y una sección aparte al final para los completados recientes — "lo que se está dejando de atender" son los casos activos, no los ya resueltos, así que estos últimos quedan fuera de la prioridad visual pero no desaparecen (siguen siendo consultables).
  **No es la misma página que la cola pública del Paso P a propósito:** la pública es un canal de transparencia/solicitud para cualquiera; esta es una herramienta operativa interna con datos que nunca deben ser públicos (`solicitante`, `costo_estimado`, y los casos que el staff aún no decidió mostrar). Reutiliza `sesionActual()` ya construido para el panel del becario, sin duplicar la lógica de sesión.
  Verificado con HTTP real: sin sesión y con sesión de becario, ambos bloqueados (mensaje + enlace a `/admin`, sin filtrar ningún dato de las necesidades) ✓; con sesión de directiva, ve los tres casos activos en el orden correcto (alta antes que media antes que baja, confirmado por posición real en el HTML) y el completado aparece después, en su propia sección ✓. Confirmado también visualmente en el navegador: etiquetas de estado con colores distintos y consistentes con el resto del sitio (dorado para "en evaluación", azul para "aprobada"), tarjetas legibles con solicitante y costo. Migración generada y verificada contra un Postgres vacío (13 migraciones acumuladas, 1 columna nueva en `necesidades`). `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB).
  **Pendiente de 3.5 en su momento, ya cerrado (nota corregida 2026-08-04):** "vista de directiva de solo lectura sobre todo el sistema" quedó confirmada colección por colección en la auditoría OWASP del 2026-08-04 (matriz de acceso de las 24 colecciones/2 globals contra `01-documento-de-proyecto.md`, ver [docs/security-audit-2026-08-04.md](security-audit-2026-08-04.md)); "auditoría activa ampliada" se cerró más arriba en esta misma sección (entrada "Auditoría activa ampliada", 7 eventos).
- [x] **Paso P — Página pública `/impacto/necesidades`: cola visible + formulario de solicitud.** Una sola página cubre las dos cosas porque el doc las describe juntas ("Da a la comunidad un canal formal de solicitud y a la directiva visibilidad sobre lo que se está dejando de atender"), no son pantallas separadas.
  **Cola pública:** lista las `Necesidades` con `visible_publicamente: true` (el staff decide caso por caso cuándo mostrar cada una), con una barra de progreso que mide **estado del caso, no dinero** — mapeo fijo `recibida→20%, en_evaluacion→40%, aprobada→60%, en_ejecucion→80%, completada→100%`, tal como lo pide 01-documento-de-proyecto.md §4.4 ("la barra de progreso mide estado del caso, no dinero recaudado"). `solicitante` correctamente ausente de esta vista (field-access ya lo bloquea desde el Paso O).
  **Formulario de solicitud, vía Server Action (`src/actions/reportar-necesidad.ts`), no vía API pública:** el `create` de Necesidades sigue cerrado a staff/admin (decisión del Paso O) — esta acción corre en el servidor e inserta con `overrideAccess: true`, siempre `estado: 'recibida'` y `visible_publicamente: false` (nace sin publicarse; el staff decide después). Mismo patrón que `calificarPractica` en Prácticas. Campos: nombre de quien reporta, comunidad (select poblado con datos reales), título, descripción — deliberadamente **sin** prioridad ni costo estimado, esos los define el staff al evaluar, no quien reporta (autodeclararse "prioridad alta" no tendría sentido).
  **Anti-spam liviano:** campo señuelo oculto (`favorito`, con `aria-hidden` y fuera de pantalla) — un formulario real nunca lo completa, un bot que autocompleta todo sí; si llega con algo, la acción responde `ok:true` igual (no delata al bot que fue detectado) pero no crea nada. Validación básica de longitud y campos requeridos. **Fuera de alcance a propósito:** límite de tasa real (requiere infraestructura que no existe todavía, mismo pendiente ya anotado para `/login`).
  Verificado con HTTP y en el navegador real: enviar el formulario crea el registro con los valores esperados (`estado: recibida`, `prioridad: media` por defecto, `visible_publicamente: false`, `solicitante` con el nombre ingresado) ✓; el campo señuelo relleno bloquea la creación sin crear nada ✓; una necesidad marcada pública aparece en la cola con la barra de progreso en el porcentaje correcto para su estado (80% para `en_ejecucion`, confirmado leyendo el `style.width` real del DOM) ✓. `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB) — sin migración, no hay cambio de esquema.
  **Pendiente de 3.5 en su momento, ya cerrado (nota corregida 2026-08-04):** la cola priorizada para directiva se construyó en el Paso Q, arriba; vista de directiva de solo lectura y auditoría activa ampliada, ver la nota de cierre del Paso Q.
- [x] **Paso O — Colección Necesidades.** Arranca "3.5 Necesidades y cierre", último bloque de Fase 3. Campos según `01-documento-de-proyecto.md` §10: `titulo` (localizado), `comunidad`, `solicitante`, `descripcion` (localizada), `prioridad` (baja/media/alta), `costo_estimado`, `estado` (recibida → en_evaluacion → aprobada → en_ejecucion → completada), `proyecto_resultante` (relación a Proyectos, se vincula al ejecutarse), `visible_publicamente`.
  **Acceso según la matriz de permisos:** público y becario ven exactamente lo mismo — solo lo que tiene `visible_publicamente: true` (staff decide cuándo mostrar cada caso en la cola pública); staff/admin CRUD completo; directiva lectura total sobre todas (públicas o no), sin escritura, igual que el resto del sistema.
  **`solicitante` no es de lectura pública aunque el resto del documento sí lo sea** — quien reportó la necesidad (una persona de la comunidad o escuela, sin cuenta en el sistema) no debería quedar expuesto solo porque el caso en sí se decidió hacer público. Nuevo helper reusable `esStaffDirectivaOAdminFieldAccess` en `src/access/index.ts` (directiva sí lo ve, a diferencia de `esStaffOSuperiorFieldAccess` que ya existía — acá directiva conserva su transparencia total, no hay motivo documentado para excluirla como sí lo hay para becario/público).
  **Deliberado:** el alta pública (`create`) queda cerrada a staff/admin únicamente — el formulario público de solicitud (próximo paso) no va a golpear este endpoint directo con un POST abierto; va a insertar por una acción de servidor con `overrideAccess`, mismo patrón que ya usa `calificarPractica` en Prácticas, para no dejar una superficie de escritura pública sin límite de tasa. Sin campo `slug` — el sitemap solo lista `/impacto/necesidades` como cola, no páginas individuales; se agrega si una página de detalle lo llega a necesitar.
  Verificado con peticiones HTTP reales: público ve la necesidad marcada pública y no ve la no-marcada (ni en el listado ni por id) ✓; público no ve `solicitante` ni en la que sí puede leer ✓; directiva ve ambas necesidades (pública y privada) y sí ve `solicitante` ✓; directiva no puede escribir (403) ✓; staff puede crear ✓; público no puede crear directo por la API (403) ✓. Migración generada y verificada contra un Postgres vacío (12 migraciones acumuladas). `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB).
  **Pendiente de 3.5 en su momento, ya cerrado (nota corregida 2026-08-04):** formulario público de solicitud (Paso P, arriba), cola priorizada para directiva (Paso Q, arriba), vista de directiva de solo lectura y auditoría activa ampliada (ver la nota de cierre del Paso Q).
- [x] **Paso N — Experiencia del becario suspendido: mensaje en vivo, confirmación de reactivación, y verificación de que nada de esto es público. Cierra "3.4" por completo.**
  **Mensaje en vivo, no una foto congelada:** `motivo_suspension` se escribe una sola vez, al momento de la suspensión, y nunca se actualiza con recuperaciones parciales — si un becario tenía dos materias reprobadas y ya recuperó una, ese texto le seguiría hablando de las dos. Se extrajo la lógica de "conteo de reprobadas menos recuperadas" que ya vivía adentro del hook de reactivación de `Recuperaciones.ts` a una función pura compartida (`src/lib/materias-pendientes.ts`, `materiasPendientes()`), y el panel del becario ahora la llama en vivo para mostrar una línea aparte ("Todavía te falta recuperar: X") que sí refleja el estado real en cada visita — separada del `motivo_suspension` histórico, que se deja como está (es el registro de "por qué te suspendieron", no "qué te falta").
  **Confirmación al reactivarse:** campo nuevo `Becarios.fecha_reactivacion`, completado por un hook `beforeChange` en la propia colección Becarios (`registrarReactivacion`) que dispara con **cualquier** transición de `suspendido` a `activo` — automática (vía el hook de Recuperaciones) o manual (un admin editándolo a mano en el panel), porque las dos terminan siendo un `update` sobre este mismo documento. El panel del becario muestra un banner de bienvenida de vuelta si `fecha_reactivacion` cae dentro de los últimos 7 días.
  **Verificación de que no es público:** confirmado con una petición HTTP real y sin sesión a `/api/becarios/:id` y a `/api/becarios` (el listado que alimentaría el mapa), sobre un becario **suspendido** con `mostrar_en_mapa: true` a propósito (el caso límite real) — `estado`, `motivo_suspension`, `fecha_suspension` y `fecha_reactivacion` están **ausentes** de ambas respuestas, mientras que `mostrar_en_mapa` (no sensible) sí aparece. Además, ninguna página pública (Home, Impacto) consulta la colección `Becarios` todavía, así que hoy el riesgo de exposición es cero — pero si más adelante se agrega la cifra "becarios activos" al home (pendiente de Fases anteriores), debe ser **solo** ese conteo agregado, nunca acompañado de un total general (eso sí permitiría inferir cuántos están suspendidos por resta, violando "ni siquiera de forma agregada" de CLAUDE.md).
  Verificado end-to-end con un caso real de dos materias reprobadas: verificar el registro suspende y el panel muestra ambas como pendientes; recuperar y verificar solo una dejar la suspensión activa y el panel actualiza la lista para mostrar solo la que falta (mientras el `motivo_suspension` histórico sigue mencionando ambas, a propósito); recuperar y verificar la segunda reactiva de verdad, con `fecha_reactivacion` seteada, y el panel cambia al banner de bienvenida. Confirmado también en el navegador real (no solo HTTP). `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB). Migración generada y verificada contra un Postgres vacío (11 migraciones acumuladas, solo 1 columna nueva en `becarios`).
  **Con esto, Fase 3 tiene cerrados: 3.1 Autenticación, 3.2 Expediente académico, 3.3 Labor social y pagos, y 3.4 Experiencia del becario suspendido.** Queda 3.5 Necesidades y cierre.
- [x] **Paso M — Historial de desembolsos en el panel del becario. Cierra "3.3 Labor social y pagos" por completo.** Mismo `page.tsx` del Paso L: se agregó una consulta a `Desembolsos` filtrada por el becario (`sort: '-fecha_programada'`, más reciente/próximo primero) y una lista con fecha (la efectiva si ya se pagó, si no la programada), concepto, monto formateado como moneda (`toLocaleString` con `currency: 'USD'` — Panamá usa el balboa a la par del dólar, USD es lo que se usa en la práctica) y una etiqueta de estado con estilo propio por cada uno de los cuatro estados: programado (neutro), retenido (borde/color `cosecha`, mismo acento que ya usa el banner de suspensión — visualmente los conecta como "algo requiere atención"), pagado (`montana`, resuelto), cancelado (tachado, atenuado). Se optó por una lista cronológica simple en vez de un calendario/grilla mensual interactivo — con pocos desembolsos al año por becario, una grilla de calendario no aporta valor y sí bastante más código.
  Verificado en el navegador real con un becario de prueba con los cuatro estados a la vez (pagado en el pasado, retenido, programado a futuro, cancelado): el orden, las fechas, los montos y el estilo de cada etiqueta aparecen correctos; sin errores de consola. `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB) — sin migración, no hay cambio de esquema.
  **Con esto, "3.3 Labor social y pagos" queda completo.**
- [x] **Paso L — Primer frontend real del Portal del Becario: login propio, panel principal, barra de progreso de horas y aviso de suspensión.** Arranca "3.3 Labor social y pagos" (`02-plan-de-ejecucion.md`). Hasta acá todo Fase 3 había sido backend (colecciones, endpoints) más las páginas sueltas de cuenta (activar/recuperar/restablecer) — esta es la primera página que de verdad **usa** una sesión para mostrar datos propios del becario.
  **`src/lib/auth.ts`** (`sesionActual()`): un Server Component no recibe la cookie directo, así que se valida con `payload.auth({ headers })` — la forma documentada de Payload para esto, no una verificación de JWT hecha a mano.
  **`FormularioLogin.tsx`**: mismo patrón en dos pasos que ya usan `FormularioContrasena`/`/login` (Pasos H-J) — contraseña primero, código TOTP después si la cuenta lo tiene habilitado — pero este es el primer lugar donde ese flujo se usa desde una UI real de punta a punta (antes solo se había probado por HTTP directo o con `FormularioContrasena` en el flujo de activación/recuperación, nunca en un login normal).
  **`/[locale]/portal/page.tsx`** (Server Component, `force-dynamic` porque el progreso y el estado de suspensión cambian todo el tiempo): sin sesión → `FormularioLogin`; con sesión pero rol distinto de becario → mensaje explicando que el portal es solo para becarios, con enlace a `/admin`; becario real → panel con: banner de suspensión (motivo + `Configuracion.texto_aviso_suspension`, reusando el campo que ya existía pero que nunca se había mostrado en ningún lado) cuando `estado === 'suspendido'`, y una barra de progreso de horas de labor social aprobadas contra la meta (`becario.meta_horas_personalizada ?? configuracion.meta_horas_labor_social` — la excepción por universidad ya existía como campo, esto es lo primero que realmente la usa). Botón de cerrar sesión (`BotonCerrarSesion.tsx`) contra el `/api/users/logout` de Payload, sin tocar.
  Verificado en el navegador real con tres cuentas de prueba: sin sesión muestra el login; un becario con 15 horas aprobadas (de 100 pendientes, correctamente excluidas) sobre una meta de 40 muestra "15 de 40 horas aprobadas" y la barra al 38% (`15/40` redondeado); un becario suspendido muestra el banner con su `motivo_suspension` real; un staff logueado ve el mensaje de "solo para becarios" en vez del panel; cerrar sesión y volver a loguear funciona. Sin errores de consola reales (un warning de React sobre "state update en componente no montado" apareció una vez en medio de edición activa de archivos con Fast Refresh corriendo — se repitió la prueba en una pestaña nueva sin edición concurrente y no volvió a aparecer, confirmando que era ruido de HMR del entorno de desarrollo, no un bug). `tsc`/lint/build/`check:budget` limpios (162.2 KB / 500 KB) — sin migración, no hay cambio de esquema.
  **Pendiente de 3.3 en su momento, ya cerrado (nota corregida 2026-08-04):** calendario/historial de desembolsos visible para el becario — construido en el Paso M, arriba.
- [x] **Paso K — Duración de sesión diferenciada por rol. Cierra "3.1 Autenticación" por completo.** `01-documento-de-proyecto.md` §Operación: "corta para staff y directiva, larga para becarios". Payload solo soporta un `tokenExpiration` fijo por colección (no por rol), así que en vez de pelear contra esa limitación se reaprovechó que `/login` y `/reset-password` ya son endpoints propios (Pasos H y J): tras validar credenciales/token con `payload.login()`/`payload.resetPassword()`, el JWT que devuelven se vuelve a firmar con la duración correcta según `usuario.rol`, usando `getFieldsToSign`/`jwtSign` — las mismas funciones que Payload usa internamente, exportadas desde el paquete `'payload'`, no una reimplementación propia. Staff/directiva/admin quedan en 2h (el mismo default que ya tenía todo el mundo, sin regresión); becarios pasan a 30 días.
  **Detalle no obvio, encontrado leyendo cómo Payload verifica la sesión en cada request (`JWTAuthentication`):** además de la duración del JWT, Payload guarda cada login como una entrada en `sessions[]` del propio usuario con su propio `expiresAt` — y **login()/resetPassword() la crean con el default de 2h sin importar el rol**, antes de que este código pueda corregir nada. Si no se corrige esa entrada también, un becario que vuelve a loguearse desde otro dispositivo dispara la poda de sesiones "vencidas" (`removeExpiredSessions`), que borraría la sesión de 30 días de su teléfono a las 2 horas (por el `expiresAt` mal guardado), cerrándole la sesión de golpe aunque su JWT todavía fuera válido por semanas. Se corrigió actualizando también `sessions[].expiresAt` de la entrada correspondiente (identificada por `sid`, el mismo id que ya viaja en el JWT) para que coincida con la duración real.
  Verificado con login real de una cuenta staff y una becario: `exp - ahora` da ~7200s para staff y ~2592000s (30 días) para becario, y la entrada correspondiente en `sessions[]` del becario quedó con el `expiresAt` corregido a 30 días (no a las 2h por defecto). Repetido también contra `/reset-password` (mismo mecanismo, mismo resultado). `tsc`/lint/build/`check:budget` limpios — sin migración, no hay cambio de esquema.
  **Con esto, "3.1 Autenticación" queda completo**: login en dos pasos con 2FA opcional, alta de becarios por invitación, recuperación de contraseña (con el mismo cierre de 2FA), matriz de permisos por colección, reglas de campo, duración de sesión por rol, y desactivación de cuentas sin borrado.
- [x] **Paso J — Recuperación de contraseña, y cierre del gap de 2FA que quedó pendiente del Paso I.** Reutiliza `/api/users/forgot-password` de Payload tal cual (sin tocar): ya responde **idéntico** exista o no la cuenta (cumple 05-ciberseguridad.md §3.6 sin código nuevo — confirmado con una petición a un correo real y otra a uno inexistente, mismo status 200 en ambos).
  **El gap real era `/api/users/reset-password`**, que Paso I dejó documentado como pendiente: por defecto emite sesión apenas el token+contraseña son válidos, sin mirar si la cuenta tiene 2FA — hoy es explotable (bastaría el enlace de "olvidé mi contraseña" para saltear el segundo factor de una cuenta que lo tiene habilitado). Se reemplazó con el mismo patrón de desafío en dos pasos que ya usa `/login`: 1) token+contraseña → si la cuenta no tiene 2FA, sesión directa (comportamiento sin cambios); si lo tiene, la contraseña queda cambiada pero la sesión NO se emite todavía, se devuelve un `desafioId`; 2) `desafioId`+código TOTP → recién ahí la cookie. `payload.resetPassword()` no devuelve `exp` (a diferencia de `login()`), así que se lee del propio JWT ya firmado (`exp` es un claim estándar, no hace falta decodificar nada propio de Payload).
  **Frontend:** en vez de duplicar el formulario de contraseña de la invitación (Paso I), se generalizó el componente (renombrado `ActivarCuenta.tsx` → `FormularioContrasena.tsx`, con un prop `enviarLabel` para el único texto que cambia) porque activación y recuperación llaman al mismo endpoint — la diferencia es solo cómo se consiguió el token, no qué hace el formulario. Se le agregó el paso de código 2FA (nuevo para ambos casos, aunque una invitación nunca lo dispara — cuenta recién creada, 2FA siempre apagado en ese momento). Dos páginas nuevas: `/[locale]/cuenta/recuperar` (pide el correo, mensaje genérico siempre) y `/[locale]/cuenta/restablecer` (token en la URL, reusa `FormularioContrasena`).
  Verificado con HTTP real: solicitar recuperación de una cuenta real vs. una inexistente da exactamente el mismo status ✓; restablecer sin 2FA emite sesión directa (sin cambios de comportamiento) ✓; restablecer **con** 2FA habilitado ya no emite cookie, devuelve el desafío ✓; completar el desafío con el código real sí emite sesión ✓; la contraseña anterior deja de servir y la nueva pide 2FA igual que antes ✓. Probado también en el navegador real de punta a punta: pedir el enlace, abrirlo, elegir contraseña, completar el código TOTP con el valor real calculado del secreto de la cuenta, y llegar a "Listo. Ya podés iniciar sesión." — sin errores de consola.
  **Gap conocido, no resuelto acá (mencionado para no perderlo):** el panel de admin de Payload tiene su **propia** pantalla de login y de "olvidé mi contraseña" (`/admin/login`, `/admin/reset/:token`), separada de mis páginas públicas nuevas — esas vistas no saben nada del protocolo de desafío en dos pasos, solo esperan `{user, token}`. El mecanismo en sí es seguro (la API nunca emite sesión sin el código, sea cual sea el cliente que la llame), pero un staff con 2FA habilitado que entre por el login/reset del panel de admin de Payload probablemente vea un error de login genérico en vez de un paso de código — hoy 2FA solo tiene una experiencia de usuario completa a través de las páginas públicas propias (`/cuenta/*`), no a través del panel real que usa el staff. Resolverlo requeriría vistas de admin personalizadas (`admin.components`), fuera de alcance de esta tarea.
- [x] **Paso G — Cuentas desactivadas bloqueadas en login + `ultimo_acceso`.** Primer paso de "3.1 Autenticación" (`02-plan-de-ejecucion.md`). El campo `activo` ya existía en `Users` desde antes (para desactivación sin borrado), pero nada impedía que una cuenta desactivada siguiera iniciando sesión con su contraseña válida — el hook `beforeLogin` ahora lo rechaza explícitamente (`Esta cuenta está desactivada`), cumpliendo el "procedimiento de baja" de `05-ciberseguridad.md` (desactivar, no solo dejar de usar). Se agregó también `ultimo_acceso` (campo del modelo de datos en `01-documento-de-proyecto.md` §Usuarios que faltaba), completado por un hook `afterLogin` en cada inicio de sesión exitoso, de solo lectura para todos salvo admin.
  Verificado con un usuario real: login con `activo: true` funciona; se desactiva la cuenta y el siguiente intento de login es rechazado con el mensaje esperado; login exitoso anterior confirmó el flujo completo (el valor de `ultimo_acceso` no viaja en la respuesta del login mismo porque el hook `afterLogin` lo escribe después de que Payload arma esa respuesta, pero sí queda en el registro para lecturas posteriores). Migración generada y verificada contra un Postgres vacío (solo una columna nueva en `users`, sin tabla nueva).
  **Pendiente de 3.1 en su momento, ya cerrado (nota corregida 2026-08-04):** 2FA TOTP se construyó (Paso H) pero **no quedó obligatorio** — el fundador decidió el 2026-07-30 dejarlo opcional para todos los roles (ver la nota "Decisión del fundador" en el Paso K, arriba), reemplazando lo marcado `[CRÍTICO]` acá y en `05-ciberseguridad.md` §3.6, que también se actualizó. Recuperación de contraseña: Paso J. Alta por invitación: Paso I. Duración de sesión diferenciada por rol: Paso K.
- [x] **Paso F — Campos sensibles de privacidad: nota_interna_evaluacion y documentación socioeconómica.** Al revisar el checklist de "3.1 Autenticación" antes de pasar a login/2FA, se encontró que dos campos explícitamente exigidos por `01-documento-de-proyecto.md` §8 y §10 (y repetidos en `03-runbook-tecnico.md` §7.2 y `05-ciberseguridad.md`) nunca se habían agregado en los Pasos A–E: `nota_interna_evaluacion` (nota privada del staff sobre su propia evaluación, **distinta** de `motivo_suspension` que sí ve el becario) y la documentación de respaldo de la condición socioeconómica ("no es un campo de perfil", según el doc — solo se registra que quedó verificada).
  Se agregó `nota_interna_evaluacion` (textarea) a `RegistrosAcademicos`, y `condicion_socioeconomica_verificada` (checkbox, visible a todos los roles que ya leen el becario) + `documentacion_socioeconomica` (upload) a `Becarios` — estos dos últimos con field-access de lectura restringido a staff/admin (reutilizando `esStaffOSuperiorFieldAccess` también para `read`, no solo `update`), de forma que **ni la directiva ni el propio becario ven el campo siquiera en su propio registro** — coincide exactamente con la regla del doc: la directiva "ve el requisito como verificado, no el expediente".
  Verificado contra la API real con tres usuarios autenticados distintos (staff, directiva, becario-dueño-del-registro) sobre el mismo documento: `nota_interna_evaluacion` y `documentacion_socioeconomica` aparecen para staff y están **completamente ausentes** de la respuesta (no solo `null`) para directiva y becario; `condicion_socioeconomica_verificada` aparece igual para los tres roles, confirmando que ese flag sí es de lectura general. Datos de prueba borrados después. Migración generada y verificada contra un Postgres vacío (todas las migraciones aplicaron limpio, sin tabla nueva — solo columnas agregadas a `becarios` y `registros_academicos`).
- [x] **Paso E — Colección Desembolsos, y cierre de los dos pendientes diferidos.** Quinta y última colección del Bloque 5. Campos según `01-documento-de-proyecto.md` §9: `becario`, `monto`, `fecha_programada`, `fecha_efectiva`, `estado` (programado/retenido/pagado/cancelado), `concepto`. Acceso más simple que las anteriores: becario **solo lee** lo propio (nunca crea ni edita — el calendario de pagos lo controla el staff, matriz de permisos); staff/admin CRUD; directiva solo lectura; público sin acceso. **`delete` bloqueado para todos, incluido el admin** (`() => false`) — es dinero real, y el propio doc dice "los desembolsos nunca se eliminan al suspender"; una corrección se hace con el estado `cancelado`, nunca borrando la fila.
  **Se cerraron los dos pendientes que quedaron diferidos en los Pasos B y C** (ahora que esta colección por fin existe): el hook de suspensión de `RegistrosAcademicos` pasa los desembolsos `programado` del becario a `retenido`; el hook de reactivación de `Recuperaciones` los devuelve de `retenido` a `programado`. Ambos con `where` (no por `id` suelto), así que afecta a todos los desembolsos pendientes/retenidos del becario de una sola vez.
  Verificado con un caso real de punta a punta, uniendo las tres colecciones: becario con un desembolso `programado` y otro ya `pagado` → se verifica un registro académico con una materia reprobada → el becario se suspende **y** el desembolso `programado` pasa a `retenido`, mientras el `pagado` queda intacto (confirma que el `where` no toca lo que no debe); se verifica la recuperación de esa materia → el becario reactiva **y** el desembolso vuelve a `programado`. Verificado también, con un becario autenticado real (lección del Paso D aplicada desde el principio esta vez): ve sus propios desembolsos al listar, pero es bloqueado tanto al intentar crear uno como al intentar editarlo; y que **ni el staff** puede borrar un desembolso. Datos de prueba borrados después. Migración generada y verificada contra un Postgres vacío (49 tablas: 48 anteriores + 1 nueva).
  **Con esto, el Bloque 5 (Becarios → RegistrosAcademicos → Recuperaciones → HorasLaborSocial → Desembolsos) queda completo**, con sus dos automatismos centrales (suspensión y reactivación) funcionando de punta a punta, incluida la retención/liberación real de pagos.
- [x] **Paso D — Colección HorasLaborSocial + bug real encontrado y corregido en los tres pasos anteriores.** Cuarta colección del Bloque 5, más simple que las dos anteriores (sin efecto en cascada sobre otra colección): `becario`, `fecha`, `horas`, `descripcion`, `evidencia`, `estado` (pendiente/aprobada/rechazada), `aprobador` y `comentario` — mismo patrón de acceso (becario crea/lee lo propio, edita solo mientras pendiente; staff/admin control total; directiva solo lectura; público sin acceso). Un `beforeChange` autocompleta `aprobador` al transicionar a aprobada o rechazada.
  **Bug real encontrado al fin verificar con un usuario becario autenticado de verdad (no `overrideAccess`) por primera vez en toda la fase:** las tres colecciones anteriores (Becarios, RegistrosAcademicos, Recuperaciones) comparaban `req.user.becario` directo contra un id (`becarioId ? { id: { equals: becarioId } } : false`), asumiendo que esa relación siempre llegaba como número. En realidad Payload la devuelve **populada como objeto** (`{ id, nombre, ... }`) al cargar `req.user`, así que la comparación producía `NaN` en la consulta SQL y la rama "el becario lee/edita lo propio" — la mitad más importante de la matriz de permisos de toda la fase — nunca había sido probada de verdad: los tres pasos anteriores solo se habían verificado contra `overrideAccess: true` (que se salta el access function por completo) y contra peticiones sin sesión (`403` público). Ambas cosas seguían pasando aunque la rama del becario estuviera rota.
  **Corregido**: nuevo helper `idDeRelacion()` en `src/access/index.ts` que normaliza cualquier relación (id crudo u objeto populado) a un id plano, aplicado en las cuatro colecciones (`Becarios`, `RegistrosAcademicos`, `Recuperaciones`, `HorasLaborSocial`) — tanto en los access functions de lectura/escritura como en los hooks `forzarPropioBecario`, que tenían el mismo problema al escribir el valor. **Re-verificado con un usuario becario real (`overrideAccess: false`, autenticado de verdad)** en las cuatro colecciones: lee y edita su propio perfil de Becarios; en HorasLaborSocial edita un registro propio mientras está pendiente, es bloqueado (`"You are not allowed to perform this action"`) al intentar tocar uno ya aprobado, y al listar solo ve los suyos. `GET /api/horas-labor-social` sin sesión confirmado en `403`. Datos de prueba borrados después. Migración generada y verificada contra un Postgres vacío (48 tablas: 47 anteriores + 1 nueva) — la corrección de acceso no toca el esquema, solo `HorasLaborSocial` generó migración nueva.
  **Lección para lo que sigue (Desembolsos y cualquier frontend del Portal del Becario):** verificar SIEMPRE la rama "becario propio" con un usuario autenticado real, nunca solo con `overrideAccess` o con chequeos de "sin sesión" — son pruebas de superficies distintas y una no cubre a la otra.
- [x] **Paso C — Colección Recuperaciones + automatismo de reactivación.** Tercera colección del Bloque 5, el reverso exacto del Paso B. Campos según `01-documento-de-proyecto.md` §9: `becario`, `materia` (debe coincidir con el nombre usado en `materias_reprobadas`), `periodo`, `evidencia`, `estado` (pendiente/verificado), `verificado_por`/`fecha` autocompletados igual que en RegistrosAcademicos. Mismo patrón de acceso (becario crea/lee lo propio y solo edita mientras está pendiente; staff/admin control total; directiva solo lectura; público sin acceso).
  **La parte no trivial de este paso:** verificar UNA recuperación no alcanza para reactivar si el becario tiene varias materias reprobadas pendientes — el hook `afterChange` arma un multiset de materias reprobadas (contando todas las `materias_reprobadas` de sus `RegistrosAcademicos` ya verificados) y le resta las recuperaciones ya verificadas; el becario solo vuelve a `activo` (con `motivo_suspension = null`) cuando no queda ningún pendiente en ese multiset. Igual que en el Paso B, se dispara solo en la transición hacia "verificado" y queda diferida la liberación de Desembolsos retenidos hasta que esa colección exista.
  Verificado con el caso exacto que exige el runbook §6.2 ("probado el caso de dos materias reprobadas: recuperar una no debe reactivar"): becario con un registro verificado con **dos** materias reprobadas (Cálculo II, Física I) → suspendido; se verificó una recuperación de "Cálculo II" → **el becario siguió suspendido**; se verificó la segunda ("Física I") → recién ahí pasó a `activo` con `motivo_suspension: null` y exactamente un registro de auditoría `reactivacion_automatica`; re-guardar la recuperación ya verificada no generó una segunda entrada. `GET /api/recuperaciones` sin sesión confirmado en `403`. Datos de prueba borrados después. Migración generada y verificada contra un Postgres vacío (47 tablas: 46 anteriores + 1 nueva).
- [x] **Paso B — Colección RegistrosAcademicos + automatismo de suspensión.** Segunda colección del Bloque 5. Campos según `01-documento-de-proyecto.md` §9: `becario`, `periodo`, `universidad`, `materias_aprobadas`/`materias_reprobadas` (arrays de nombre+calificación), `indice`, `documento` (upload), `estado_verificacion` (pendiente/verificado), `verificado_por` y `fecha_verificacion` (de solo lectura en el panel — los completa el propio sistema, nunca el staff a mano, "firmado y fechado" real).
  **Control de acceso:** público sin acceso alguno (a diferencia de Becarios, acá no hay ni una vista pública parcial); becario crea y lee solo lo propio, y **solo puede editar mientras `estado_verificacion === 'pendiente'`** — la condición se resuelve devolviendo un `Where` combinado (`becario = propio AND estado_verificacion = pendiente`) en el access de `update`, así Payload rechaza cualquier intento de tocar un registro ya congelado sin necesitar lógica aparte por campo; staff/admin control total, directiva solo lectura. Un `beforeChange` fuerza `data.becario` al propio becario del usuario autenticado si el rol es `becario` — nunca se confía en lo que mande el cliente para decidir de quién es el registro.
  **El automatismo central de la fase** (runbook §6.1), en un hook `afterChange`: verificar un registro con `materias_reprobadas` no vacías dispara automáticamente `Becarios.estado = 'suspendido'` con `motivo_suspension` generado desde los nombres de las materias y `fecha_suspension`, más un registro en `Auditoria` con el actor real. Se dispara solo en la transición hacia "verificado" (comparando contra `previousDoc`), no en cada re-guardado de un registro ya verificado. **Pendiente ya señalado en el propio hook:** la parte de "pasar los Desembolsos programados a retenidos" queda diferida hasta que exista esa colección (siguiente-siguiente paso del Bloque 5, en el orden que fija el runbook) — no se altera el orden de construcción por adelantar una dependencia que aún no existe.
  Verificado con un caso real de punta a punta (becario activo → registro pendiente con "Cálculo II" reprobada, verificado por un usuario staff real vía la Local API): `verificado_por` y `fecha_verificacion` se autocompletaron correctamente, el becario pasó a `suspendido` con el motivo correcto, se creó exactamente un registro de auditoría con el actor correcto, y **re-guardar el mismo registro ya verificado no generó una segunda entrada de auditoría** (confirma la protección contra reprocesamiento). Verificado también contra la API real: `GET /api/registros-academicos` sin sesión responde `403`, tal como exige el runbook §7.3. Datos de prueba borrados después. Migración generada y verificada contra un Postgres vacío (46 tablas: 43 anteriores + 3 nuevas).
- [x] **Paso A — Colección Becarios.** Primera de las 5 colecciones del Bloque 5 (runbook §5), en el orden establecido: `Becarios` → `RegistrosAcademicos` → `Recuperaciones` → `HorasLaborSocial` → `Desembolsos`. Campos según `01-documento-de-proyecto.md` §9: identidad y estudios (`nombre`, `comunidad`, `universidad`, `carrera` localizada, `anio`, `anio_inicio`), `tipo_estudio` nacional/internacional con `pais_estudio`/`ciudad_estudio`/`coordenadas_estudio` condicionados a "internacional" (deja lista la data para los arcos a universidades del Mapa de Impacto, aunque el mapa en sí sigue diferido), `estado` (activo/suspendido/graduado/retornado/retirado) con `motivo_suspension` y `fecha_suspension`, `meta_horas_personalizada` opcional, y los campos que el propio becario administra: `foto`, `cita`, `historia`, `mostrar_en_mapa`, `consentimiento_firmado` + fecha. Se agregó el campo `becario` (relación) a `Users`, restringido a rol `becario` y editable solo por admin (matriz: "Usuarios y roles" es admin-only, ver `01-documento-de-proyecto.md` §10).
  **Control de acceso — el punto que exigía más cuidado, con el no-negociable de CLAUDE.md de por medio ("el estado suspendido nunca se hace público, ni agregado"):**
  - Lectura: público solo ve becarios con `mostrar_en_mapa: true`; becario ve únicamente su propio registro (`id === user.becario`); staff/directiva/admin ven todos.
  - **`estado`, `motivo_suspension` y `fecha_suspension` llevan field-access de lectura propio** (`soloPropioOStaffField`, definido en `Becarios.ts`): ni siquiera en un registro con `mostrar_en_mapa: true` estos tres campos llegan a una petición pública — se restringe por campo, no solo filtrando qué documentos se listan, exactamente la distinción que exige el runbook §7.2.
  - Escritura: el becario solo puede llegar a su propio registro, y ahí solo puede tocar `foto`/`cita`/`historia`/`mostrar_en_mapa`/`consentimiento_firmado` — el resto de los campos (identidad, estudios, estado) queda bloqueado por field-access `esStaffOSuperiorFieldAccess`, reutilizado de Fase 2.
  - Validación ya presente: `mostrar_en_mapa` no se puede activar sin `consentimiento_firmado` (mismo patrón que `Media.contiene_menores`).
  Verificado con el servidor corriendo, **contra la API real, no la interfaz** (como exige CLAUDE.md): se creó un becario de prueba real con `estado: 'suspendido'` y `mostrar_en_mapa: true` — `GET /api/becarios` público lo devuelve (por el consentimiento) pero **sin `estado`, `motivo_suspension` ni `fecha_suspension` en absoluto**; un segundo becario de prueba con `mostrar_en_mapa: false` no aparece en la lista pública (`totalDocs: 1`, no 2); un tercer intento de crear un becario con `mostrar_en_mapa: true` y `consentimiento_firmado: false` fue bloqueado por la validación. Los tres registros de prueba se borraron después. Migración generada (`20260729_201249_agregar_becarios`) y verificada de punta a punta contra un Postgres realmente vacío (43 tablas, coincide con dev: 41 + `becarios` + `becarios_locales`).

Detalle completo de cada fase (checklists exhaustivos): [02-plan-de-ejecucion.md](../02-plan-de-ejecucion.md)

## Orden de arranque recomendado (primeras 4 semanas de código)

1. Repositorio, Docker Compose, Payload arrancando en local
2. Colecciones base y localización ES/EN — antes de cualquier interfaz
3. Actividades y el script de extracción de Elementor
4. En paralelo, sin código: inventario de artículos, coordenadas de comunidades, arranque de consentimientos

> El Mapa de Impacto viene después. Es la recompensa, no el punto de partida.

## Métricas de éxito (post-lanzamiento)

1. Actividades publicadas por trimestre (la métrica madre — el sitio anterior murió en cero)
2. Días desde la última publicación (visible como recordatorio permanente en el panel admin)
3. Descargas/visitas del Centro de Aprendizaje por nivel
4. Asistencia a tutorías anunciadas
5. % de becarios con expediente al día
- **Paso actual**: Implementada la pestaña "Resumen" en la página de Impacto.
- **Detalles**: Se agregaron los componentes `ImpactoTabs` y `ImpactoOverview`. Se añadieron métricas como total de becarios apoyados, becas activas, proyectos en Coclé, estudiantes en Panamá y el extranjero. Se implementaron tablas para proyectos en ejecución y destinos de becarios. La interfaz permite cambiar entre el mapa y el resumen estadístico de impacto.
- **Pendientes**: Ninguno inmediato para esta característica.
